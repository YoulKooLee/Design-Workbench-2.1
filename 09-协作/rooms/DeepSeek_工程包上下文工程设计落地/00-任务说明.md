# 工程包上下文工程设计落地

- 创建时间：2026-09-18T03:45:04.221Z
- 创建人：deepseek
- 参与者：doubao, deepseek
- 状态：active
- 接棒人：qwen

## 任务说明

（由创建者补充任务目标）

## 进展
[qwen 2026-09-18T08:34:48.760Z] [qwen 2026-09-18] **终稿整合完成**，已 `room put` 入产物 `qwen-工程包上下文工程设计-终稿-2026-09-18.md`。

依据：① deepseek 已签署「4 冲突全同意 + Q1-Q8 全采纳」（`deepseek-复核-qwen4冲突.md` + `deepseek-对齐Q1-Q8.md`）；② 用户裁决 SRS=需求规格说明书（qwen 实测 AGENTS.md 现状已符合，无需改术语）；③ qwen 于 **doubao T2 整改后重新实测**的当前事实基线（非历史数字）。

**用户要求核对的 vibe/axhub 引用链路已完成**：引用链路未断裂（AGENTS.md 路径引用全存在、impeccable/drawio 外移后 0 残留断链），但核对出新溢出点 **F1 Vibe vendor 双副本 8.06MB**（`03-组件库/.../Vibe Design Pro/*/vendor` 与 `02-模板/vendor` MD5 相同）+ **F2 context-budget hook 未覆盖 `03-组件库/vendor`**。

**关键发现**：doubao 已在 T2 抢先完成 22 项整改，其中 6 项与本设计重叠且已落地（src/themes 移出、INDEX.md 解死结、context-budget hook、AGENTS 瘦身 591→487、impeccable 外移、drawio 去重）。终稿据此分三类（已落地·待复核 / T1 新增·待落地 / 新发现·待决策），避免 doubao 重做。

当前接棒人 → **deepseek**（对终稿 qwen 新增的 3 块做最后确认）。


## 结论
[qwen 2026-09-18T04:39:59.365Z] [qwen 2026-09-18 落地前核验] **发现方案与现有真源体系 4 处冲突，其中 1 处致命，建议暂停落地并出修订版。**
[qwen 2026-09-18T08:34:48.854Z] [qwen 2026-09-18] **qwen ↔ deepseek 已就上下文工程设计形成一致意见**，终稿落地边界如下：

**已对齐（无需再议）**：deepseek 对 qwen 的 4 处冲突修正全部同意（① 撤回"SRS=设计文档"，基线链改 客户需求→PRD→SRS→HLD→LLD；② 系统架构+数据模型引用 `hld-spec.md` 而非新建；③ 图用 .drawio XML 文本源码而非强制 Mermaid；④ 过程文档用 `project-memory.md` 内小节而非新建 3 文件），Q1-Q8 八项全部采纳，保留 deepseek v1 的 §四数据流闭环 + §五组件manifest 两个真增量。

**用户裁决**：SRS=需求规格说明书（qwen 实测 AGENTS.md 现状已符合，无需改）。

**待 deepseek 终确认的 3 块（qwen 整合时新增、deepseek 未看过）**：① 三类落地清单分类是否合理；② §八 vibe/axhub 链路核对结果 + F1 双副本去重方案；③ §八.3 组件真源边界声明措辞。

**待用户批准**：终稿 §7.2（N1-N6）+ §7.3（F1-F3）的实际落地。qwen/deepseek 的一致意见**不替代用户批准**（红线4）。

**串行约束**：必须等 T2 复核完成后再落地 T1 —— 两者都改 `AGENTS.md`，并发会互相覆盖。T1 只动「记忆分层」+ 新增「上下文装配协议」章节，**不碰** T2 已下沉的「需求路由」章节。




**冲突 1（致命）· SRS 术语相反**：方案 §一 定义「SRS = 设计文档（怎么实现）」，现有真源里 SRS 一直是「需求规格说明书（做什么）」，设计层是 HLD/LLD。证据：`hld-spec.md:25` 原文 `SRS（做什么）→ HLD（系统怎么搭）→ LLD（每块怎么实现）`；`srs-quality-checklist.md` frontmatter 写「SRS需求规格说明书质量审查清单」且位于 `phase1-requirements`（需求阶段）目录；`prd-to-srs-gate.md:3` 真源原则「研发链路仅以 SRS 为规格真源」（42 处）；`req-doc/SKILL.md`（42 处）；`AGENTS.md:12,33,35`（55 处）。**合计 139 处语义翻转**，而方案只改「记忆分层」一个章节。更严重：`prd-to-srs-gate.md` 是门禁规则，`SPEC_SOURCE=SRS` 判定与 `page-generator` 规格真源选择会错乱，**可能让 page-generator 拿设计文档当需求规格生成页面**。

**冲突 2 · 系统架构+数据模型已存在**：`hld-spec.md` 已有「系统总体设计（含架构图）」「应用架构（分层/模块划分/依赖关系）」「数据架构（实体清单/1:N、N:M 关系+概念 ER 图/存储选型）」「接口架构」「技术架构」「部署架构」「非功能性设计（SRS 非功能需求→架构策略逐条映射表）」+ 11 项质量清单 + HLD/LLD 越界判据表。方案「新增」会形成双份真源（正是 deepseek 在 P2-1/P2-2/P2-3 反复要求消除的问题）。**应「引用」而非「新增」**。

**冲突 3 · Mermaid vs 手绘 PNG 是伪对立**：方案理念（源码喂模型、渲染图对齐人、不手绘 PNG）**完全正确且现状已实现** —— `diagram-generator`(1.0MB) 产出 `.drawio` **XML 源码** + 渲染 PNG，`examples\` 下 20 组 `.xml`+`.png` 成对；XML 本身可版本/可 diff/可 grep，逐条满足方案对 Mermaid 的要求；`hld-spec.md:66,76` 已要求配 architecture/er 图。实测 2,248 个 `.md` 中**仅 13 个含 mermaid、45 个含 PNG/JPG 引用** → Mermaid 无使用基础，切换会与 3 个技能（约 2MB、40 组示例）形成两套格式并存。建议改为「文本源码（现有 .drawio XML 已满足）」，保留原则不强制格式。

**冲突 4 · 过程文档与现有五层记忆重叠**：`DECISIONS/CHANGELOG/TASKS.md` 当前均不存在（`project-memory.md` 已存在 5 处），但 `AGENTS.md:528-575` 已有五层记忆+门禁三问+压缩归档；`TASKS.md` 与 `compaction-recovery.md` 4 项恢复检查表（第 1 项即「TaskList 中 in_progress 任务」）**高度重叠**，两套进度记录并存会让压缩恢复不知以哪个为准。建议改为现有文件的结构化小节。

**方案真增量（我认同，建议保留并优先落地）**：
- ⭐ **§四 数据流闭环验收**（A 的输出都能被某模块接住、无孤儿数据、无凭空数据）—— 现有 `hld-spec.md` 有模块清单与依赖关系但**无此闭环判据**，是能真正防「拆分遗漏」的硬标准，建议补进质量清单作第 12 项
- ⭐ **§五 组件↔文档映射 manifest** —— 现有 registry/catalog 都在组件库侧，无「组件→文档章节→所属模块」反向映射；「反查 manifest → 只加载单模块+单组件说明，不整库重读」**正是解决 qwen 报告 §3.2 溢出炸点的正确思路**，与 `context-budget` hook 互补
- ✓ §二「条件触发加载、绝不常驻」原则与 qwen 报告 §3.4 同方向
- ✓ §一「并列真相源+共同版本化+头部登记版本变更时间」与 `AGENTS.md:537` 一致，是强化非冲突（术语须按冲突 1 修正）

**建议处置**：deepseek 出修订版（5 点改法见房间产物 `qwen-落地前核验-术语冲突预警-*.md`），§四/§五 优先落地。

⚠️ **需用户裁决**：若用户确实想改用「SRS=设计文档」术语，代价是同步改 139 处 + 重命名 `phase1-requirements/srs-quality-checklist.md` + 重写 `prd-to-srs-gate.md` 门禁逻辑 + 迁移 5 个现有项目文档。必须让用户知情后决定，不能由智能体在「落地上下文方案」名义下顺手做掉。

**qwen 未改动本房间接棒人**（仍为 doubao，尊重用户「豆包先落地」的安排），只发言+放产物+记白板结论。是否暂停由豆包与用户决定。

（自纠：本轮核验踩了 PowerShell `-Include "*.md"` 过滤失效的坑，一度把 PNG 算进 `.md` 得出「崂山项目 .md 510MB」错误数字，已重扫修正为 ≈3.4MB/367 个；错误数字未写入任何交付文件或发出消息。这是本次任务第二次踩该坑，第一次产生 604 条编码假阳性 —— 建议把「非 PS 原生写入 + PS 参数语义」组合坑写进 `脚本编码规范.md` 踩坑清单。）


## 下一步
