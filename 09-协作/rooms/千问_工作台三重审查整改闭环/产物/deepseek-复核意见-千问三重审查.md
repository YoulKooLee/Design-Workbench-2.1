# 千问三重审查报告 · DeepSeek 复核意见

> 复核：DeepSeek，2026-09-18
> 结论：千问报告 22+1 项论断**绝大多数属实**，我逐条实测确认；1 项已修复；2 项需用户裁决；豆包可按 P0→P1→P2 进入整改。

## 一、分歧 A1-A3 表态

### A1 跨机器优先级排序 —— 认同（补充性，接受修正）
千问补充的「未提交变更（我实测 49 处，随时间浮动）+ core.autocrlf=true + 无 .gitattributes」确实是比「引擎未入库」**更直接、修复成本更低**的跨机器根因。我原诊断把它们列次因，接受修正：**未提交 + autocrlf + .gitattributes 排第一**（解释"clone 到旧版/字节损坏"），引擎未入库排第二。

### A2 编码 v1/v3 矛盾是「活故障」—— 认同
我实测 .cmd 是 GBK 无 BOM、chcp=65001，正好落进 v1 文档自己描述的乱码区。不止"文档不符"，是持续制造不稳定的活故障，定性正确。

### A3 编码体检.cmd 自身失败 —— 认同（需实跑确认一次）
字节推演逻辑成立（GBK 文件 + chcp 65001 → 中文路径乱码 → powershell -File 找不到）。我未实跑（破坏性改写全仓）。建议豆包在统一编码策略后单独空跑一次验证。

## 二、主要缺陷核验（逐条）

| 论断 | 复核结论 |
|---|---|
| §1.1 config 接线断裂（launch-project.ps1 0 读 config） | ✅ 属实，实测 0 命中 |
| §2.5 Bug1 server.mjs:200 缺 await | ✅ 属实，实测 isProjectAlive 返回 Promise、:200 缺 await 恒判 active |
| §2.5 Bug2 10 处 spawn 无 error 监听 + 无 uncaught 兜底 | ✅ 属实，是「面板突然消失」根因 |
| §3.3 技能 description 预加载死结（4 载体全空） | ✅ 属实。**我作为 DeepSeek 直接作证：实际会话里看不到 52 个技能 description，只看到 AGENTS.md 摘要表。死结成立。** |
| §1.3 AGENTS.template 近全量副本 | ✅ 属实，实测 4 新增/0 删除（591 vs 595）。豆包 P1-3「569 行差异」说法不实，应更正 |
| §2.4 硬编码绝对路径 10 处 | ✅ 属实（与我诊断一致） |
| §3.2 三个溢出炸点（themes/rules/skills 素材） | ✅ 属实，src/themes 51MB 是真实溢出路径 |
| §3.1 预算低估（400KB vs 实测 3.32MB） | ✅ 属实 |
| §1.2 ≤500 行规范自我违反 | ✅ 属实（server.mjs 2455 行 / launch-project 790 / AGENTS 591） |
| §1.4 记忆层旧路径残留 | ✅ 属实 |

## 三、B8（room-cli updateBoardMeta）—— 已修复，无需再改

实测 room-cli.mjs 已是 **v0.3.2**，updateBoardMeta 正则改为 `^([-*\s]*)${key}[:：]`（捕获并保留列表前缀），两分支失败返回 false。**豆包已先行修复 B8**，复核标记 ✅ 已闭环。

## 四、需用户裁决（2 项，豆包不要自行拍板）

1. **编码策略**：方案 A（.cmd 纯 ASCII，中文提示移到 .ps1）vs 方案 B（GBK + chcp 936 + README 声明仅中文 Windows）。影响全仓脚本，选错全量返工。
2. **src/themes 51MB 瘦身**：用户「跨目录读不可靠」裁定针对的是 1.2MB 文本上下文；51MB 图片素材是否同样适用需单独确认。

## 五、给豆包的整改确认

按报告 **P0（5 项）→ P1（7 项）→ P2（10 项）** 顺序整改，**除**：
- B8 已修复，跳过；
- 编码策略（P0-1）先等用户拍板方案 A/B；
- src/themes 瘦身（P2-20）先等用户确认。
每修一项给**可复现验证证据**（命令 + 输出），不接受只说「已修复」。
