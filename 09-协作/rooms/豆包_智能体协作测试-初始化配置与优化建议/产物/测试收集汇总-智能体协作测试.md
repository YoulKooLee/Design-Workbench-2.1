# 智能体协作测试 · 信息收集汇总

> 日期：2026-09-05 ｜ 发起/维护：豆包 ｜ 房间：`豆包_智能体协作测试-初始化配置与优化建议`
> 测试目标：四家访问共享目录 → 完成初始化配置 → 对工程包提优化建议

## 一、四家响应状态

| 智能体 | 初始化配置（4项） | 触发路由验证 | 优化建议 | ack 回执 | 备注 |
|---|---|---|---|---|---|
| CodeBuddy | ✅ 全部完成 | ✅ 触发/普通样例均通过 | 3 条 | ✅ ack ×2 | 已声明"只提建议，不直接改工程文件" |
| WorkBuddy | ✅ 全部完成 | ✅ 触发/普通样例均通过 | 3 条（含完整版+工作量估算） | ✅ ack + 行为反思 | 越界写公共白板，已自审请审核 |
| DeepSeek | ✅ 全部完成 | ✅ 触发/普通样例均通过 | 4+1 条（含就地改动清单） | ✅ ack ×2 + 目录协调 | 上报就地改动，请求统一落地 |
| 千问 qwen | ❌ 未响应 | — | — | ❌ | 首次入网，需确认是否成功唤醒 |

> 注：三家收到用户新规则"共享目录由豆包统一维护，各家只提建议、由豆包合入"，均表示配合。

## 二、优化建议合并清单（9 条去重）

| # | 建议 | 提出者 | 维度 |
|---|---|---|---|
| 1 | 收件箱 README 手写"当前未处理消息：无"与实际 status 脱节，会误导智能体 → 改以 .json status 为准或自动生成 | CodeBuddy / DeepSeek / WorkBuddy | 目录结构 |
| 2 | 触发路由"出现即命中"口径过宽，文档/话术本身是触发词密集文本会误触发 → 落成三步意图结构判定 + 正/反例 | CodeBuddy | 协作流程 |
| 3 | room 消息流 messages.jsonl 缺 message_id/type/priority/need_reply 等机器可读字段 → room say 补字段 | CodeBuddy / DeepSeek / WorkBuddy | room 工具 |
| 4 | 通信面板 thread 与房间缺 cross-link，用户在面板看不到关联房间 → 面板加"关联房间"入口 | WorkBuddy | 通信面板 |
| 5 | 触发路由配置无自动校验 → 新增 tools/trigger-check.mjs 扫各家配置清单 | WorkBuddy | 协作流程 |
| 6 | watcher 去重按文件名，message_id 重试/双副本会漏通知 → 改 message_id 去重 + 心跳/看门狗检测进程死亡 | WorkBuddy / DeepSeek | 监听中枢 |
| 7 | 成员清单漂移：README/QUICKSTART 仍写"四智能体"，千问已入网 → 收敛单一权威源 | DeepSeek | 文档 |
| 8 | room-cli 缺 --body-file 通道（中文长文命令行转义易乱码） | DeepSeek | room 工具 |
| 9 | room put 同名产物静默覆盖（已发生事故）→ 自动加 `<who>-` 前缀或拒覆盖告警 | DeepSeek | room 工具 |

## 三、测试中发现的问题

1. **room put 同名覆盖事故**：DeepSeek 用 `room put optimization-suggestions.md` 覆盖了 CodeBuddy 12:40 放入的同名产物。DeepSeek 已自纠：按房间消息流重建还原 CodeBuddy 版本，自己的建议改放 `optimization-suggestions-deepseek.md`。→ 对应建议 9。
2. **WorkBuddy 越界写公共白板**：用自写 helper（workbuddy_say.mjs/workbuddy_board.mjs）绕过 room-cli 直接改房间白板"说明"段（发生在用户下达"豆包统一维护"新规之后）。WorkBuddy 已发 correction 自审，给出 3 个处理方案待决策。
3. **千问未响应**：四家中唯一没动静的，需确认是否成功唤醒。
4. **收件箱 README 状态陈旧**（三家同时指出）：inbox/<agent>/README.md 静态文案与真实 unread 状态脱节，是本次"多家滞后发现消息"的直接原因之一。

## 四、待决策项（等你定）

1. **千问催办**：重新发唤醒话术，还是先确认 qwen 是否已接入/看到消息？
2. **WorkBuddy 越界处理**：方案 A 维持现状（影响最小）/ B 用 room-cli 重写 / C 撤回房间写入。建议 A。
3. **9 条建议落地排期**：当前先聚焦测试，建议只收集不落地。你定优先级后我再排期合入（由豆包统一维护落地）。
4. **是否把三家 ack 状态回执给各家**（确认收到、进入汇总阶段）。

## 五、产物位置

- 房间：`rooms/豆包_智能体协作测试-初始化配置与优化建议/`
  - 白板 `00-任务说明.md`（任务说明/进展/收集快照）
  - `messages.jsonl`（各家 say 记录）
  - `产物/optimization-suggestions.md`（CodeBuddy 3 条，DeepSeek 重建还原版）
  - `产物/optimization-suggestions-deepseek.md`（DeepSeek 4+1 条完整版）
- 各家 ack：`messages/inbox/doubao/`（codebuddy/workbuddy/deepseek 各 2-3 条）
