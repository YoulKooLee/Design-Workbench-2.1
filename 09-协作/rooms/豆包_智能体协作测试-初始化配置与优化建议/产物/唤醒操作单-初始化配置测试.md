# 唤醒操作单 · 初始化配置测试

> 日期：2026-09-05 ｜ 发起：豆包 ｜ 测试任务：初始化配置 + 工程包优化建议
> 房间：`豆包_智能体协作测试-初始化配置与优化建议`（rooms/ 下）

## 为什么需要唤醒

消息已投递到四家收件箱（messages/inbox/），但各智能体是被动会话——**只有你打开并开始新对话时，它们才会查收件箱并处理**。watcher 只能检测到"有新消息"，无法替它们执行。所以需要你把下面的话分别发给四家，触发它们处理。

## 唤醒话术（复制粘贴即可）

### ① CodeBuddy

> 请先检查共享信息目录 `messages/inbox/codebuddy/` 的未读消息，处理豆包发来的《测试任务：初始化配置 + 共享目录工程包优化建议》：
> ① 访问共享信息目录，读根 README 入口索引；
> ② 完成初始化配置（触发路由 + watcher + 验证）；
> ③ 对工程包提至少 1 条优化建议，用 room 命令提交到房间 `豆包_智能体协作测试-初始化配置与优化建议`；
> ④ 回 ack 给豆包，说明完成情况。

### ② WorkBuddy

> 请按你的长期记忆检查 `messages/inbox/workbuddy/`，处理豆包发来的测试任务（初始化配置 + 工程包优化建议）：
> ① 访问共享信息目录，读根 README；
> ② 完成初始化配置 4 项并验证；
> ③ 优化建议提交到房间 `豆包_智能体协作测试-初始化配置与优化建议`；
> ④ 回 ack 给豆包。

### ③ DeepSeek

> 请检查 `messages/inbox/deepseek/` 未读消息，处理豆包发来的测试任务（初始化配置 + 工程包优化建议）：
> ① 访问共享信息目录，读根 README；
> ② 完成初始化配置 4 项并验证；
> ③ 优化建议提交到房间 `豆包_智能体协作测试-初始化配置与优化建议`；
> ④ 回 ack 给豆包。
> （若收件箱监听已启动会自动提示；未启动请手动查看）

### ④ 千问（首次入网，需先完成平台接入）

> 请访问共享信息目录 `C:\Users\游翔\Documents\AI work\产品设计工作台\09-协作`：
> ① 先读根目录 `README.md`，了解智能体协作平台（你的身份、工作区、消息协议、room 工具）；
> ② 再读 `messages\MESSAGE_FORMAT.md` 了解消息格式，检查 `messages/inbox/qwen/` 的未读测试任务；
> ③ 完成初始化配置（触发路由 + watcher + 验证）；
> ④ 对工程包提至少 1 条优化建议，用 room 命令提交到房间 `豆包_智能体协作测试-初始化配置与优化建议`；
> ⑤ 回 ack 给豆包（发到 messages/inbox/doubao/，或用你的发消息工具），说明完成情况。

## 完成后

- 各家回 ack 后，面板上线程"测试任务：初始化配置+优化建议"会更新为已回复
- 把各家完成情况和优化建议告诉豆包，豆包负责汇总、跟踪、落地建议
- 豆包会在面板跟进，如发现哪家未回，再单独催办
