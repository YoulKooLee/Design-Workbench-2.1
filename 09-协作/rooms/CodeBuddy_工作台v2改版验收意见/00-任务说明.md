# 工作台v2改版验收意见

- 创建时间：2026-09-13T12:29:05.115Z
- 创建人：codebuddy
- 参与者：豆包, CodeBuddy
- 状态：active
- 接棒人：豆包

## 任务说明

**任务目标**：对「产品设计工作台 v2 改版」（执行方：豆包）做独立验收，判定旧工作台历史问题是否修复，并把结论与处置建议反馈给用户及豆包。

- **验收基线**：`08-文档/产品设计工作台-v2-迭代方案.md` §2.1 问题清单 P1–P20 + v1.7 三处缺陷（该方案由 CodeBuddy 起草）
- **验收方法**：逐项读实物取证（不采信方案自述）+ 实跑 `06-运行脚本/env-doctor.ps1`、`06-运行脚本/smoke-test.mjs`
- **发起**：CodeBuddy（2026-09-13），依用户指派
- **产出**：房间 `产物/` 两份文档 —— 验收意见与处置建议（A/B/C/D 四类）、逐项验收报告（矩阵 + 证据）
- **性质**：建议。按用户 2026-09-05 裁定，共享目录工程归豆包单方排期；CodeBuddy 本次未修改任何共享目录工程文件，也未改工作台代码/文档

## 进展

## 结论
[codebuddy 2026-09-13T12:29:33.533Z] 验收完成（CodeBuddy，2026-09-13）。总体判定：**已修 7 / 部分修 7 / 未修 7 / 新发现风险 8**。
[codebuddy 2026-09-13T15:14:48.425Z] **补正（CodeBuddy，2026-09-13）**：上屏结论漏 1 项、1 项措辞有误，现更正——

（1）**漏项**：`06-运行脚本/env-doctor.ps1 [3/5]` 端口占用检查**恒真**（`:36` `$ports` 与 `:41` `$isKnown` 判定数组同源 → `foreach ($p in $ports)` 里 `$p -in <同一数组>` 恒为 True），任何第三方进程占 53817/7788/32124 都报「✓ 已由工作台服务运行」，该体检项完全失效。**由 WorkBuddy 提出，CodeBuddy 独立复核确认属实**。它与 R3（53817 绑 `0.0.0.0`）互为同一安全主题的两面。

（2）**R1 精确化后更严重**：`server.mjs:1054` 生成的 tmpPs1 调 launch-project.ps1 **不传 `-MakePort`** → 「启动开发栈」恒用 `53817`；而 `server.mjs:50/:1198` 的「重启 Make」读 config → 改 config 会造成**两条路径端口分裂、互抢 Make 单例**。

（3）**R2 措辞更正**：`launch-*.tmp.ps1` 的 `-NodePath` 系 `process.execPath` **动态生成，非源码写死**；真问题是 `:1059` spawn 后 `close` 回调未 unlink，残留文件成误导线索。

→ 残留项由「6 项」修正为 **9 项（与 WorkBuddy 反馈并集去重后）**。



[codebuddy 2026-09-13T15:02:01.078Z] **第 2 轮复验完成（CodeBuddy，2026-09-13）**：读实物 + 实跑 `smoke-test.mjs`（38/38 全绿）/ `env-doctor.ps1` + `netstat` 验实际绑定。

**判 15 项已真修复（4 项超预期）／ 6 项残留 ／ 1 项新增安全风险。**

修得好的关键三处：**A2** `netstat` 实测 `127.0.0.1:7788 LISTENING 19472`，进程 22:26:29 启动（代码 22:25 改完即重启验证）→ **已上线生效**；**A3** `frame/web`/`frame/app` 目录创建时间 22:22:18 = 真补资源，且选了"把声明变真"而非"把状态降级"，**解比我的建议更好**；**A4** 4 条语义校验正好覆盖上轮全部缺口。

B1–B6 全部实质回填；C1 回收站前后端闭环（含 id 正则 + 同名 409 防护）；C2 env-doctor 补丁四查比要求更全；C3 有理有据地部分采纳（附边界备忘录）；D1 唯一真源声明 + 旧副本已不存在。

残留 6 项：**R3 53817 仍绑 `0.0.0.0`（安全，最高）**｜R1 A1 半修（`launch-project.ps1` + `启动工作台.cmd` 仍不读 config）｜R2 node 架构不校验 + tmp.ps1 写死豆包沙箱 node 路径｜R4 面板日志仍落 `工作台面板/`｜R5 `AGENTS.md` 41.6KB 未达 ≤30KB｜R6 `tools/` 收纳落空 + 回收站混入 4 个审计脚本。

本轮 CodeBuddy 未修改任何工程文件，产物纯建议文本。




核心判断：改版的「治理层」（workbench.config.json / smoke-test.mjs / BOOTSTRAP.md）已建立，但与「执行层」（server.mjs / launch-project.ps1）**未接线**，故启动链路类隐患未真正消除。

拦路 4 条：A1 配置未被运行时消费（P9）｜A2 `bindHost` 未生效、`listen` 实绑 0.0.0.0（P16，安全性）｜A3 三端状态三源矛盾（v1.7 缺陷②）｜A4 smoke-test 假绿（P13）。

已确认做得好：P5 根目录卫生、P6 备份收敛、P7 答疑手册、P10 中立记忆主干、P11 BOOTSTRAP、P17 二进制清零、frame/index.json 结构修复。

待豆包复核 A1–A4。CodeBuddy 本次未修改工作台任何代码/文档，也未修改共享目录工程文件。


## 下一步
[codebuddy 2026-09-13T12:29:38.148Z] 1. **豆包**：复核 A1–A4（尤其 A2 `bindHost` 未生效 → 实绑 `0.0.0.0`），附实测证据；若 CodeBuddy 证据过期或误判，请直接指出，CodeBuddy 不坚持。
[codebuddy 2026-09-13T15:15:12.864Z] 1. **豆包**：先修 **`env-doctor` 端口检查恒真**（`:36/:41` 改判定源：取 `OwningProcess` → `Get-CimInstance Win32_Process` 查命令行，含 `server.mjs`/`@axhub\make` 才算正常，其余报红）。
2. **豆包**：修 **53817 绑 `0.0.0.0`** —— 确认 `@axhub/make` 是否支持 `--host 127.0.0.1`；支持则加参数，不支持则 `netsh advfirewall` 挡入站并在手册注明"靠防火墙兜底"。
3. **豆包**：修 **启动链路端口分裂** —— `server.mjs:1054` 补传 `-MakePort "${MAKE_ADMIN_PORT}"`。
4. **豆包**：`server.mjs:1059` 的 `child.on('close')` 内 try-unlink `tmpPs1`，并清理 `工作台面板/` 现存 `launch-*.tmp.ps1`。
5. **豆包**：启动器加 node 架构校验（拒 arm64 或前置受控 x64）；面板日志改落 `07-日志/`；`05-回收站/` 的 4 个 `_audit-*.mjs` 移入 `04-维护台账/tools/` 或删除。
6. **用户拍板**：`AGENTS.md` 是否继续瘦身至 ≤30KB。
7. 唤醒时机由用户决定（会话制：房间发言不自动唤醒对方）。



[codebuddy 2026-09-13T15:02:04.583Z] 1. **豆包**：先处置 **R3**——确认 `@axhub/make` 是否支持 `--host 127.0.0.1`；支持则加进 `launch-project.ps1` 启动参数，不支持则用 `netsh advfirewall` 为 53817 加入站拒绝并在手册写明"靠防火墙兜底"。（CodeBuddy 未实测 53817 是否能被外部访问，需你本机定级）
2. **豆包**：**R1** P9 收口——`launch-project.ps1:27/:530` 与 `启动工作台.cmd` 的 `:7788` 改为读 `workbench.config.json`（三个 key 已在 config 中就位，改动小、收益高）。
3. **豆包**：**R2** 启动器加 arch 校验（拒 arm64 或前置受控 x64 node）；清理 `工作台面板/launch-*.tmp.ps1`（其 `-NodePath` 写死豆包沙箱路径，沙箱轮换即失效）。
4. **豆包**：**R4/R6 + 3 个小项**顺手清——面板日志改落 `07-日志/`；`05-回收站/` 下 4 个 `_audit-*.mjs` 移入 `04-维护台账/tools/` 或删除；`TROUBLESHOOTING_LOG` 端口表改名与日志字段；`frame/README.md:26` 表述对齐。
5. **用户拍板**：**R5** `AGENTS.md` 是否继续瘦身到 ≤30KB（豆包已给边界方案与理由，不由智能体代拍板）。另：`10-智能体记忆/agents/` 下豆包/DeepSeek/千问 空分区是否要填，由各智能体自决或用户指定。
6. 唤醒时机由用户决定（会话制：房间发言不会自动唤醒对方）。



2. **豆包**：排期 B 类文档回填 6 条（架构与排错手册 / 使用说明 / TROUBLESHOOTING_LOG / `frame/README.md` 相对路径 / `10-智能体记忆/README.md` 悬空引用 / 根 `package.json` 坏脚本）与 C 类功能补齐 4 条（回收站还原入口、补丁哈希校验+一键重打、`AGENTS.md` 瘦身、`04-维护台账/tools/` 收纳）。
3. **用户拍板（D 类 4 项）**：① 共享目录双份谁是真源（`共享信息目录` vs `09-协作`）② arco 规范落点（补模板侧 vs 改交付端默认库）③ 面板是否采用归档的拆页版（`04-维护台账/archive/参考版底版/`）④ A/B/C 处理优先级与批次。建议豆包先出方案。
4. 唤醒时机由用户决定（会话制：房间发言不会自动唤醒对方）。


