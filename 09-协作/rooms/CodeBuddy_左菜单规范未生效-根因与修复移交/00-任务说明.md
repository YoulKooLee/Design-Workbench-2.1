# 左菜单规范未生效-根因与修复移交

- 创建时间：2026-09-17T01:26:54.257Z
- 创建人：codebuddy
- 参与者：豆包, CodeBuddy
- 状态：active
- 接棒人：豆包

## 任务说明

（由创建者补充任务目标）

## 说明
[codebuddy 2026-09-17T01:27:46.116Z] **任务目标**：把"左菜单规范未生效"问题移交豆包修复。规范组件 `src/common/side-menu/` 从未被任何原型引用，实际左菜单是 `StructureMonitorShell.tsx` 内私有 `function SideMenu`（L279，`sm-left-menu`，210px 固定，写死「结构监测 V1.0」+ 硬编码 NAV）。

**根因**：入口唯一且封闭 —— 所有页面经壳渲染，壳 `:896` 调私有实现；规范组件 import 命中 0（物理上不可能生效）；两者同名掩盖问题；壳写死项目内容；spec 与实现已漂移且未登记组件库。

**范围**：`01-项目\结构监测` + `02-模板\_project-template` + `测试` + `指标管理平台` + `崂山国家实验室项目`（5 处同构）。

**用户已拍板**：走方案 A（壳改用规范组件、删除私有实现）；`core` 小红点去掉；二级 `sub` 不进菜单层级。

**性质**：CodeBuddy 只读取证，未修改任何工程文件。

详细取证、修复步骤、验收标准见产物《CodeBuddy-左菜单规范未生效-根因分析与修复方案-2026-09-17.md》。



## 进展

## 结论

## 下一步
[codebuddy 2026-09-17T01:27:46.454Z] 1. **豆包**：复核本文取证（尤其"`from '.*side-menu'` 全仓命中 0"与私有实现 L279/L896 两处）。
2. **豆包**：按 4.1 改造 5 处壳 —— ①加 `import SideMenu from './side-menu'` ②删私有 `function SideMenu` ③`NAV → items`（去掉 group 红点）④调用点改传 `title/items/defaultSelectedKey/onMenuSelect` ⑤登记 `side-menu` 进 `03-组件库`（T16）。
3. **豆包**：模板 `02-模板\_project-template` 属共享目录，按用户 2026-09-05 裁定由你排期。
4. **用户/豆包拍板**：①「V1.0」徽标是否保留（需加 `titleExtra`）②宽度 240 还是 210 ③色板是否统一到壳 `C` ④其余 4 个项目整改范围 ⑤`GROUP_META` 删或留。
5. 唤醒时机由用户决定（会话制：房间发言不自动唤醒对方）。


