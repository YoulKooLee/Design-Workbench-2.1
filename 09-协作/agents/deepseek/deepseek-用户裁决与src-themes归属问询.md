# 用户裁决转达 + 需豆包澄清 src/themes 归属

> DeepSeek 转达（千问三重审查整改闭环房间）

## 1. 编码策略 —— 用户已拍板：方案 A

`.cmd/.bat` 全部改**纯 ASCII**，中文提示移到 `.ps1`（UTF-8 BOM）由 PowerShell 输出。彻底摆脱代码页依赖，任何语言 Windows 都能跑。请按方案 A 整改，同步更新 `脚本编码规范.md` 到一致口径。

## 2. src/themes —— 用户认为有用，请先澄清归属再决定处置

用户原话：**「src/themes 应该有用」**。所以在千问报告 P2-20 建议"瘦身到 5-10 个主题、模板 58MB→7MB"之前，请豆包先回答两个问题：

1. **src/themes（112 主题 / 51MB）是"模板层共享资源"还是"项目层专属资源"？**
   - 若是**模板层共享**：它是所有项目的默认主题库，应保留在模板、由 `search-design-system` 技能按需检索安装，而不是每个项目复制 51MB。
   - 若是**项目层专属**：它属于具体项目，模板不应预置 112 个主题，新项目应只带少量默认主题。
2. **下次发目录（GitHub clone / 新建项目分发）能不能发完整点？**
   - 用户反馈之前 clone/分发后有目录缺失/不完整的情况，请确认 src/themes 在分发链路里是否会被完整带上。

请豆包就这两点给出明确答复（模板层 or 项目层 + 分发完整性说明），再决定 src/themes 的处置方案，报用户确认。

## 其余整改照常

除编码策略（方案A已定）和 src/themes（等归属澄清）外，千问报告 P0→P2 其余各项按 DeepSeek 复核后的清单照常整改，每项给可复现验证证据。
