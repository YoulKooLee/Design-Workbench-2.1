# 04-维护台账/tools/ — 临时与诊断脚本收纳

> 本目录收纳工作台**临时 / 一次性诊断脚本**（用完即删或留档均可），避免散落在工作台面板/ 等运行目录。

## 登记

**当前：暂无登记脚本（本目录为空）。**

> 常驻诊断脚本**不在这里**，它们在各自职责目录，勿重复收纳：

| 脚本 | 所在位置 | 说明 |
| --- | --- | --- |
| `smoke-test.mjs` | `06-运行脚本/` | 工作台冒烟自测（41 项），改动后必跑 |
| `env-doctor.ps1` | `06-运行脚本/` | 启动前环境体检（Node/环境变量/端口/组件库/补丁） |
| `launch-project.ps1` | `06-运行脚本/` | 项目开发栈启动（8 步），核心脚本不挪动 |

本目录只收**临时 / 一次性诊断脚本**（用完即删或留档），长期脚本请归位上表。

## 约定

- 临时脚本**不得**放在 `工作台面板/`、根目录等运行位置；放本目录或直接用后删除。
- 诊断脚本若指向绝对路径，须在头部注明适用范围（如"旧 Axhub 根专用"），失效即删。
- **"收纳"= 移动到本目录（或登记说明），不是删除**。若某脚本确无继续价值，先问用户再删。
- 2026-09-13：`工作台面板/check-deps.mjs`（自述"用完即删"、指向旧 Axhub 根）已清理。
- 2026-09-13：05-回收站 曾混入 `_audit-api/_audit-docs/_audit-inbox/_audit-validate.mjs` 4 个审计脚本（违反本约定），现回收站已清空、无残留；后续审计类脚本一律落本目录登记。
- 2026-09-14：`工作台面板/server-console.log`（54B，旧实例遗留；新实例已改写 `07-日志/server-console.log`）已清理——**面板目录只留 `server.mjs` / `public/` / `package.json`**，出现其它文件先判断是否为残留。

