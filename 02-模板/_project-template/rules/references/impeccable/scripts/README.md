# impeccable scripts（已外移）

工具代码已移至 `02-模板/vendor/impeccable-scripts/`（千问 P2-18：从工程包上下文区移出 1.3MB 可执行脚本，保留 SKILL.md 与 reference 准则）。

引用路径：`../vendor/impeccable-scripts/<file>`（相对 `_project-template` 根）。
