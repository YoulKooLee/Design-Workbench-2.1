# 项目记忆

> 本项目专属记忆。**优先级最高**：与工作台通用规则（Agent_Stack）冲突时，以本文件为准。
> AI 在本项目内开发时，先读本文件再动手；日常踩坑先记入「二、项目经验」，不急着进 Agent_Stack。

## 一、项目画像

- **定位**：<一句话描述本项目是什么>
- **功能需求**：
  - <要点清单，可引用 docs 下的需求文档>
- **设计风格**：
  - <色板 / 字体 / 风格词，可引用 DESIGN.md 等>
- **开发规范**：
  - <技术栈 / 目录结构 / 编码约定>
- **技术约束**：
  - <如：大屏比例 16:9、pnpm 限制、特定 CDN、后端接口约定等>

## 二、项目经验

> 本项目踩坑记录，一条一组。格式：`日期 | 场景 | 根因 | 修复 | 验证`。
> 判定可跨项目复用时，提炼为 Agent_Stack 规则（scope: universal/workspace）并在「三、结项状态」登记规则 id。

- 2026-08-18 | <场景> | <根因> | <修复> | <验证>

## 三、过程层（T1 落地 · 压缩可丢 · 永不进基线）

> 以下三个小节为**过程层**：讨论、试错、被否决方案、任务进度等**不得写入基线**（PRD/SRS），一律落在这里。
> 压缩时可丢，但 `[否决]` 条目**必须保留理由一行**。写入规则见 AGENTS.md「写入侧三态门禁」。

### 决策记录（过程层·压缩可丢·永不进基线）

> 一条一组，格式：`日期 | 决策 | 理由 | 状态(采纳/否决/挂起)`。否决必填理由。

- 2026-09-18 | <决策> | <理由> | <状态>

### 变更流水（过程层·压缩可丢·永不进基线）

> 一条一组，格式：`日期 | 变更 | 触发人 | 影响范围`。

- 2026-09-18 | <变更> | <用户/智能体> | <影响范围>

### 任务进度（过程层·与 compaction-recovery.md 对齐）

> 与 `compaction-recovery.md` 恢复检查表第 1 项对齐：TaskList 中 in_progress 任务。
> 有且仅有一个 in_progress；压缩恢复时以此为准。

- <进行中任务>：<当前步骤> | <下一步>

## 四、结项状态

- **结项日期**：
- **提炼到工作台层的方法论**：<规则 id 或「无」>
- **归档位置**：<`Agent_Stack/05-archive/结项/<项目名>-<日期>/` 或「无」>
