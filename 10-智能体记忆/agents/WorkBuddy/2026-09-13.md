# 2026-09-13 工作日志

## 产品设计工作台 v2 优化方案（初稿 → 三轮修订）

### 第一轮：旧工作台盘点

- 盘点旧工作台 `AI work\Axhub`：资产（server.mjs 零依赖后端、launch-project.ps1 8 步链路、_project-template、10-智能体记忆/Agent_Stack 记忆层、排错手册）+ 9 条痛点（启动链路脆、NODE_OPTIONS 注入、记忆体系分裂、前端单文件膨胀、目录卫生差、备份散乱、文档空心、编码纪律靠人肉、端口静态）。
- 产出《产品设计工作台-v2-优化方案.md》于新工作区根目录。
- 环境备注：本会话 Bash 工具坏（coreutils 缺失），PowerShell stdout 被吞，需写临时文件再 Read；Glob 对 `AI work` 根目录超时。

### 第二轮：朋友参考版借鉴分析

- 参考 `AI work\知识库\工作台（参考）`（5 文件：server.mjs 42KB + app.js 79KB + index.html + style.css + start.bat）。
- 核心差异：参考版是「反代同源 + iframe 一体化」——整体反代 Make（SSE 流式），增强走 /wbapi/*，原型预览走 /proto/<projectId>/ 同源反代（iframe 内 DOM 可访问），不改 Axhub 源码。
- 8 个借鉴点 J1-J8：依赖复用 deps/ensure（robocopy /MT:16 145MB≈1s + 锁文件 hash + 增量补 + 回滚）、/proto/ 代理（Referer + lastProtoProjectId 会话兜底 10min）、附注模式 annotations.json 随 git 走、看门狗 20s 重拉、AI Agent 一键安装 + npm 全局 PATH 注入 + 项目级 wb-agent.json + /api/ai/runs 注入、WB_BUILD_ID 自动刷新 + 侧栏三态 localStorage、git checkout/分组提交/单文件还原、路由分层顺序。
- 不宜照搬：参考版没处理 NODE_OPTIONS 注入、stdio ignore 静默失败、会话兜底单项目假设、无项目目录约定/模板/端口池。
- 方案修订：v2 底座切换为参考版代码，我们的 NODE_OPTIONS 清洗/日志/端口池/01-项目 目录约定合并进去。

### 第三轮：vibepm-dev-agent v1.7 资产整合

- 用户要求把 `更新资料\vibepm-dev-agent-v1.7` 迭代进方案，重点是 frame/ 需求前端组件，要求可拓展、可维护、产品设计工程可调用。
- 包内容：frame/（admin 48 page-type 母版三件套 + 11 组件演示 + gallery + _meta/pages.catalog.json）；.agents/skills 30+（srs-writer/feature-dev/annotation/pm-* 等）；knowledge（conventions 14 篇 + ui-libs 5 库）；agents/rules/hooks。
- 关键认知：frame 消费模型是「对照重写」（非 npm 依赖、禁运行时引用、禁改母版）——与 Axhub Make AI 变异工作流天然契合；frame 真正价值是 page-type 信息架构，与技术栈无关。用户项目是 React+TSX+Tailwind，母版是 HTML/Vue-Arco，只能对照重写。
- **发现 bug：v1.7 的 frame/index.json 是坏 JSON**（web/app 两段缺 key，18/29 行），迁入时必须修复。
- 整合设计：落位 `04-组件库/frame/` 工作台级唯一真源；server.mjs 加 /frame/* 静态路由；面板新增「组件库」页（page-type 列表+预览+应用到项目）；调用约定写进 _project-template（点名 page-type→读三件套→按项目栈重写继承 pro-* 结构→禁换皮）；视觉冲突约定：母版只借结构，配色以项目 .style/ 优先。
- 技能去重原则：用户已装重名 skill 优先（prd-writer/prototype-to-prd/brainstorming/skill-creator 等），vibepm 独有高价值迁入（srs-writer/annotation/feature-dev 改造版），pm-* 第二批，knowledge 并入工作台知识库，adapters/hooks 不迁。
- 附注两套分工：参考版=轻量批注（一期），vibepm annotation=SRS 驱动正式需求标注（二期评估，契约 v17，.annot-xxx class 注入）。
- 方案文档已加第 3 节，章节顺延为 1-7，决策点 7 个。

## 待办

- 等用户对 7 个决策点拍板后按一期（底座融合）开工。
- 注意：本会话 .workbuddy/memory/ 曾出现文件丢失（写入后读取不到），已重建。

## 晚间：用户两条指令执行（记忆迁移 + 痛点核查）

- **指令 1**：.workbuddy 记忆文件 → `10-智能体记忆\agents\WorkBuddy\`。确认 2026-09-13.md 已在位且完整（豆包重构时已放入）；根目录 .workbuddy 仅剩 workspace.json（IDE 状态，不动）。**自此 WorkBuddy 项目记忆写入 `10-智能体记忆\agents\WorkBuddy\`，不再写根 .workbuddy/memory/**。
- **指令 2**：豆包执行了 v2 改版（面板拍板沿用旧 axhub-manager 体系更名"工作台面板"，非参考版底座），我按 P1-P9 逐条核查并实测（smoke-test 34/34 通过、env-doctor 5 段全绿、面板 API/Make 健康 HTTP 200、试点项目指标管理平台 8.8s 启动 done）。
- 核查结论：P1/P2/P3/P8 已修复；P5/P6 基本修复（残留：tmp.ps1 不删、server-console.log 在面板目录、面板根目录散工具脚本）；P7/P9 部分修复（答疑手册已回填但排错手册仍是 08-11 旧版；config 存在但 server.mjs/cmd 不读它，端口仍硬编码）；P4 拍板保留旧体系未变（index.html 62.6KB 单文件）。
- 新发现 bug：env-doctor [3/5] 端口检查 $isKnown 恒 true，任何进程占 53817/7788 都报"工作台服务运行"，失去告警意义；launch-project.ps1 第 113 行注释与逻辑表意相反（实际是传播清洗后状态）。
- 收件箱（已迁至 09-协作/messages/inbox/workbuddy）有 5 条未读（08-31~09-05），含 2 条需 ack，已上报用户待指示。

## 深夜：豆包调整后二次核验

- **已修复确认**：① P9 server.mjs 已接 workbench.config.json（PORT/MAKE_ADMIN_PORT = env || config || 默认），smoke-test 新增 bindHost 检查；② P7 排错手册已更新为 v2 版（09-13 21:30），无 axhub-manager/08-11 旧路径残留；③ launch-project.ps1 113 行注释改为"透传"，与逻辑一致；④ 面板根目录 check-deps.mjs 等杂物已清；⑤ smoke-test 从 34 项增至 38 项全过。
- **仍未修复**：🔴 env-doctor [3/5] `$isKnown` 仍恒真（豆包只改注释未修逻辑，实测任何进程占 53817/7788/32124 均报"已由工作台服务运行"，已用 PID 命令行查证实锤；建议校验进程命令行含 server.mjs 或 @axhub/make）；🟡 tmp.ps1 即产即删未实现（残留 launch-*.tmp.ps1，server.mjs child close 后不 unlink）；🟡 server-console.log 仍写在面板目录（启动 cmd 40 行）而非 07-日志；🟢 启动/停止 cmd 标题仍为 "Axhub 工作台启动器/停止器"；🟢 cmd 层 7788/53817/32124/8899 端口硬编码未读 config。
- 附加发现：当前 7788 面板进程由豆包沙箱 node 启动（非启动 cmd），功能正常不构成问题。
- 踩坑：Write 工具写 ps1 无 BOM 会被 PowerShell 5.1 当 ANSI 解析致中文串错乱（"字符串缺少终止符"），需手工加 BOM（内容首字符 U+FEFF）。

## 深夜补记：反馈已发豆包 + 两处自纠

- **消息已发**：经 msg-cli send（correction/high/cc user/action do_task）把二次核验 4 项遗留发给豆包，回读豆包 inbox 副本验证内容完整无乱码。
- **发现 CodeBuddy 同轮独立复验**（15:02Z，先我 8 分钟发豆包）：6 项残留 + 1 新增安全风险 R3（Make 53817 绑 0.0.0.0 全网卡暴露，面板已收敛但 Make 未）；其 R2 指出 tmp.ps1 内写死豆包沙箱 node 路径（沙箱轮换即失效）——两份反馈互补，豆包会收到合并视图。
- **自纠 1**：上轮 Remove-Item 删 05-回收站 4 个 _audit-*.mjs 实为静默失败（exit 无输出、文件仍在，我误报"已清理"）。本轮用 node fs.unlink 成功删净并验证回收站为空。
- **自纠 2 / 踩坑**：PowerShell Remove-Item 对部分文件会静默失败（命令 exit 1 且无任何输出），**删除文件后必须 Test-Path 回验**；删除失败时改用 node fs.unlinkSync 兜底。
