# 2026-09-15 工作日志

## 脚本代码异常审查（用户指令"检查产品设计工作台所有脚本文件，看有没有异常的"）

范围：工作台自有运维/启动/通信/维护脚本——`06-运行脚本`、`工作台面板`、`04-维护台账`、`09-协作/messages/tools`、根启动器（启动/停止工作台.cmd、_git-scan.mjs）。排除 node_modules 与 `01-项目`/`02-模板`/`03-组件库` 应用源码、`.agents` skill 脚本。流程：先用子代理全量只读审查，再亲自读码核验关键项。

### 🔴 高危（已亲自读 server.mjs 坐实）
- `工作台面板/server.mjs`：`relative` 取自 HTTP 查询/body（路由 `/api/projects/history` GET、`/git-init` `/git-commit` `/git-rollback` `/git-delete` POST），仅经 `isDemoRelative()`（只查 demo.flag）即 `path.join(AXHUB_ROOT, relative)` 后进入 `exec(\`git -C "${dir}" …\`)`（行 523/536-545/556）。
  - 无 `startsWith(AXHUB_ROOT)` 边界守卫 → `../` 路径穿越逃逸工作区；
  - 未转义引号 → `relative` 含 `"` 与 shell 元字符可破 `git -C "..."` 引号注入命令。
  - grep 确认无 `Authorization`/`app.use`/`middleware` 鉴权；仅绑 `127.0.0.1`（行 33/2019）。属本地 RCE 风险面（恶意网页 CSRF/重绑定到 localhost:7788 即可触发）。
  - 已做对的部分：`hash` 经 `^[0-9a-f]{4,40}$` 校验（行 890/901）；commit message 用 `-F` 临时文件（行 874-879）。
  - 修复：加 `safeResolve(relative)` = `path.resolve(AXHUB_ROOT, relative)` 后必须 `startsWith(AXHUB_ROOT + sep)` 且拒绝 `"`/控制字符，再传给 git 函数。
- 同源中危：DELETE `/api/projects`（~1011）、`/open`、`/context/current`、`/projects/owner`（~1015/1086/1386/1465）同样仅 `path.join` 无边界，可被 `../` 穿越后移入回收站或改写 `project-memory.md`。

### 🟡 中危
- `工作台面板/launch-01-项目指标管理平台.tmp.ps1` 残留：server.mjs 应在 child 退出时删临时 ps1（~1143 `unlinkSync`），仍残留说明上次启动进程被强杀/崩溃、清理未执行；文件内 NodePath 写死 `22.22.2-3`。需确认面板是否正常退出。
- `04-维护台账/patches/apply-active-guard.mjs`：第 4 行把补丁目标写死到**另一个工作区** `Axhub/01-项目/结构监测/node_modules/@axhub/make/dist/server/cli.mjs`；`patch-manifest.json` 备份锚点是 `bin/cli.mjs`——补丁锚点≠备份锚点，且指向旧 Axhub 工作区，属跨工作区写死遗留。建议移出/删除。

### 🟢 低危/约定（子代理报，部分经我纠偏）
- `DSH-InboxWatcher.vbs`：Read 直接读中文正常、无 U+FFFD（子代理"损坏"系误报）；仅写死 `C:\Program Files\nodejs\node.exe` 系统 node 路径（便携性提示，非损坏）。
- `启动/停止工作台.cmd`：GBK 编码无 BOM，本机 GBK 代码页正常，但与仓库 UTF-8+BOM 约定不一致，仅 UTF-8 工具链处理时乱码。
- `msg.ps1` 缺 BOM，与同目录其它 .ps1 约定不一致。
- `inbox-watcher.mjs` / 各启动器写死 `C:\Users\游翔\…` 机器绝对路径（个人便捷脚本，便携性低）。
- `06-运行脚本/launch-project.ps1` 429/549 冗余死代码（`if(-not $NodePath)` 永不触发）。
- `msg-panel-server.mjs` 头声明 v2.2 但日志打 v2.1。
- `04-维护台账/patches/make-cli.mjs.pre-patch.bak` 11.5MB 第三方打包备份入库（仓库膨胀）。

### 无异常（子代理确认）
smoke-test.mjs、env-doctor.ps1、msg-cli.mjs、room-cli.mjs、agent-hub-watcher.mjs、archive-tool.mjs、onboard-agent.mjs、patch-manifest.json。
未发现明文密钥/token/密码硬编码（grep 命中均为 .bak 内 TS 编译器 `Token` 标识符）。

### 处置
用户仅要求"检查"，未要求修复。高危项已给出修复方案，待用户确认是否动手。

## 交接给豆包修复（用户指令"发给豆包处理"）
- 已用 msg-cli 把审计清单 + 修复分工发给豆包（`to=doubao`，`cc=user` 留痕）。
- `message_id`：`20260915T075245Z-workbuddy-产品设计工作台脚本代码异常审查与修复`
- `thread_id`：`workbench-script-audit-fix`｜类型 `task`｜优先级 `high`｜`due_at` 2026-09-18｜`action=fix-script-anomalies`
- 正文含：🔴高危（server.mjs 路径穿越+命令注入，给 safeResolve 方案）、🟡中危（tmp.ps1 残留、跨工作区补丁脚本）、🟢低危清单、不改动项、执行要求（不动真实数据交互、`workbench.config.json` 唯一配置源）。
- 副本已落 `inbox/doubao` + `inbox/user` + 我的 `outbox/workbuddy`。执行闸门：豆包面板需你批准才动手。改完回报后我可介入截图核验渲染/回归。
