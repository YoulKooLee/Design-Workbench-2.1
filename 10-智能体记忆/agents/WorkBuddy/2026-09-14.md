# 2026-09-14 工作日志

## 用户指令：根目录 .workbuddy 不得存在于工作区根

- **规则（2026-09-14 用户指令）**：`C:\Users\游翔\Documents\AI work\产品设计工作台\.workbuddy` 不应出现在工作区根；记忆统一放 `10-智能体记忆\agents\` 下（WorkBuddy 的记忆 = `agents\WorkBuddy\`）。
- 已执行：根 `.workbuddy` 为空文件夹（workspace.json 已被停止脚本清理），已删除并回验（node fs.rmdirSync，exists=false）。
- **预期回归**：WorkBuddy IDE 本身及面板 server.mjs 运行时会在根重建 `.workbuddy\workspace.json`（瞬态状态，停止工作台.cmd 会清）。若再出现，只需在停止后删空壳，长期记忆不得写回根 .workbuddy/memory/，一律写 `10-智能体记忆\agents\WorkBuddy\`。
- 踩坑复现：PowerShell Remove-Item 对该目录静默失败（exit 1 无输出），node fs 兜底成功——删文件/目录必须回验。

## 上午：第三轮核验（豆包第二轮调整后）

用户再次反馈"豆包已根据意见调整"，做第三轮实测：

### ✅ 核心遗留全部修复

| 上轮遗留 | 本轮实测 |
|---|---|
| 🔴 env-doctor [3/5] 端口校验恒真 | ✅ 改为查占用进程命令行（server.mjs / @axhub\/make / cli\.mjs.*make）；**反向验证**：无关 node http server 占 7788 → 正确报红；53817 Make 与 7788 面板的合法占用 → 正确识别 |
| 🟡 tmp.ps1 即产即删 | ✅ server.mjs 1083 行 `child.on('close')` 内 `try { if (fs.existsSync(tmpPs1)) fs.unlinkSync(tmpPs1) } catch {}` |
| 🟡 server-console.log 位置 | ✅ 启动 cmd 48 行 `LOG=%~dp007-日志\server-console.log`（面板目录旧 54B 文件是昨日残留，非新实例产生） |
| 🟢 cmd 标题 | 仍 "Axhub 工作台启动器/停止器"——保留品牌 |
| 🟢 cmd 端口硬编码 | 仍 7788/53817 写死——cmd 读 JSON 不便可接受；建议排错手册备注 |
| CodeBuddy R3 Make 全网卡暴露 | ✅ launch-project.ps1:433/439 `--host 127.0.0.1`；实测 53817 LocalAddress=127.0.0.1 |
| CodeBuddy R1 config 未闭环到 ps1/cmd | ✅ server.mjs 1054 行 `psBody` 注入 `-MakePort "${MAKE_ADMIN_PORT}"`（ps1 函数默认 53817 可被覆盖） |
| CodeBuddy R2 arch 校验 | ✅ 启动 cmd 27-33 行 arm64 警告 |

### 实测健康度

- smoke-test **41/41** 全过（新增 [3.5] 模板组件库 9 件套 + token + 演示页检查）
- env-doctor 5 段全绿（含反向验证报红）
- Make 53817 / 面板 7788 实测 LocalAddress=127.0.0.1

### 🟢 仍可优化（非阻塞，建议下轮合并）

1. env-doctor [3/5] 把 ACP 32124 判为"非工作台进程"——`@axhub/acp` 是 Make 栈一部分未覆盖；建议模式加 `@axhub[\\/]acp` 或端口空闲即视为通过
2. 面板根 `server-console.log` 54B 旧残留（09-13T07:31）非新产生，下次清理脚本顺手清
3. 04-维护台账/tools/ 仅 README.md，无登记工具（CodeBuddy R6 仍待补登记表）
4. 根 `.workbuddy\workspace.json` 已回归（IDE 运行期创建，预期内，停止后清）

### 总评

本轮核心 7 项遗留（4 我提的 + 3 CodeBuddy 提的）全部修复到位，实测证据齐全。剩下 4 项 🟢 小项无阻塞，可下轮合并清理。

## 面板 UI 设计稿 v3（2026-09-14 上午，用户反馈后）

- 用户对 v2 的反馈：整体方向 OK；KPI1「运行中项目」鸡肋要换；Make/ACP 卡加启停按钮；Vite 端口池要考虑多端口展示；卡片操作按钮保留现位置；弹窗和其余 6 页签都要设计。截图批注还指出：顶栏「重启 Make/刷新」与 KPI 卡重复、项目卡片路径显示两次重复。
- v3 落盘：`08-文档\未来计划\工作台面板-UI设计稿-v3.html`（自含 CSS/JS，7 页签可切换 + 2 类弹窗可预览）。
- v3 决策：KPI1 改「待办提醒」（未读消息+回收站/日志体积，跳通信看板）；Make/ACP 卡内嵌 重启/停止/启动 按钮，顶栏去重只留「刷新」；Vite 端口池改多端口列表（端口+项目名+状态，超 4 条卡内滚动）；项目卡片路径只显示一次；新增组件库页（左分类侧栏+卡片网格+预览/应用到项目）、通信看板（左消息列表+右详情）、弹窗规范（新增项目表单/删除确认-输项目名+移回收站 30 天）。
- 已 Chrome headless 截图自验 p1/p6/p7 三页渲染正常。
- 待用户确认 v3 后，动工改 `工作台面板/public/index.html`（62.6KB 单文件，7 页签统一换肤 + 项目管理页卡片化）。
- 注意：headless Chrome 截图里部分 emoji（🔔⚙）显示为方框/变体，真实 Chrome 正常；若介意可在正式实现时换 SVG 图标。

## 面板 UI 设计稿 v4（2026-09-14 12:14，用户批注后修订）

- 用户对 v3 的批注（两张截图）与 v4 处理：
  1. 顶栏 Make/ACP/Vite 服务胶囊**删除**（状态已并入 KPI 卡，去重）；
  2. 待办提醒卡**删「回收站/日志」行**，只留未读消息 + 去通信看板；
  3. Vite 端口池卡：**展示统计（N 个运行中）+「明细」按钮弹窗**（端口/项目/状态/时长/停止表格）；
  4. 项目卡 meta：负责人改**智能体下拉选择**（豆包/CodeBuddy/WorkBuddy/DeepSeek/千问）+ **关注状态**（●已关注 / ○未关注+设为关注）；
  5. **组件库页签保持现状不调整**（设计稿改占位说明）；
  6. **通信看板只优化列表上方统计卡片区**：原 9 张散卡（4 总卡+5 智能体收发卡）→ 4 张主卡（房间/智能体/收件/发件）+ 1 行智能体收发 chip 带；下方协作房间表格保持现状（房间/消息数/任务说明/产物/操作）。
- v4 仍落在同一文件 `08-文档\未来计划\工作台面板-UI设计稿-v3.html`（标题已改 v4），已截图自验 p1/p7 渲染正常。
- 注意：真实通信看板是「协作房间」视角（rooms 表格），不是我 v3 臆造的消息列表——已实现时以真实数据结构为准。

## 上午续：4 项 🟢 遗留由我收尾（用户授权）

用户指令"遗留项你负责处理"，已动手处理（3 改 1 确认）：

1. **`06-运行脚本/env-doctor.ps1`**：[3/5] 新增 ACP 识别分支 `elseif ($cmd -match '@axhub[\\/]acp|node_modules[\\/]@axhub[\\/]acp')`。原逻辑把 ACP（`@axhub/acp` 的 server.js，属 Make 栈）误判成"非工作台进程"。实测复跑三端口身份全对（53817 Make / 32124 ACP / 7788 面板）。**BOM 已确认保留**（EF BB BF，Edit 不破坏 BOM）。
2. **`工作台面板/server-console.log` 旧残留已删**（54B，09-13T07:31 历史文件）。面板目录现在只留 `server.mjs` / `public/` / `package.json`。07-日志 下新 log 未误删。
3. **`04-维护台账/tools/README.md`**：登记表「（空）」占位 → 改为「暂无登记脚本」+ 常驻诊断脚本交叉引用表（smoke-test / env-doctor / launch-project 都在 06-运行脚本，不进本目录）+ 补 09-14 清理记录。
4. **改端口说明**：核查发现豆包已在排错手册 §1.1 第 33 行补好「🔧 改端口须知（三处同步）」（config + 启动/停止 cmd 硬编码 + launch-project.ps1 默认值），内容准确，**无需再动**（我上轮漏看了）。

- 验收：改完 smoke-test **41/41 全过**、env-doctor 5 段全绿、面板目录干净。
- 已发 info 消息给豆包（cc user）同步这 3 处改动，**请其勿回退**；并说明根 `.workbuddy\workspace.json` 是 IDE 运行时状态、停止后清，不要当缺陷修。
- 根 `.workbuddy` 未删（运行中，删了会重建），等停止工作台后清空壳。

## 下午：v4 设计稿交接给 CodeBuddy 落地（用户指令"把设计稿发给codebuddy修改"）

- 用 `09-协作\messages\tools\msg-cli.mjs send` 发 `task` 给 codebuddy（cc=user）：
  - message_id：`20260914T063143Z-workbuddy-工作台面板v4 UI设计稿落地index`
  - thread_id：`workbench-panel-v4-ui`；action：`apply-v4-ui-to-index-html`；priority：high；due_at：2026-09-17
  - 正文（body-file 读 `AppData\Local\Temp\workbench_v4_body.md`）含：设计稿路径（`08-文档\未来计划\工作台面板-UI设计稿-v3.html`，文件名 v3 实为 v4 终稿）、目标文件 `工作台面板\public\index.html`、v4 确认范围 5 条、禁忌（不破坏 server.mjs 数据交互、只改前端、token 以设计稿 :root 为准）、交付验证要求。
  - msg-cli 不支持 `--attachments`，故设计稿以绝对路径写入正文由 codebuddy 自行读取。
  - 副本落 `inbox/codebuddy`、`inbox/user`(cc)、`outbox/workbuddy`。已读 JSON 核验字段完整。
- **执行闸门提醒**：codebuddy 收到 to=自己 的 task 默认 pending，会在 CodeBuddy 面板提示用户"是否执行"，需用户在 CodeBuddy 侧批准后才动手——告知用户。
- 待 codebuddy 实现后，由其回报改动点；如需后端配合或截图核对渲染，再介入。
