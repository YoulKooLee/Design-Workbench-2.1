# WorkBuddy · 2026-09-17

## 一、会话开始：查收协作信箱

- 查 `09-协作\messages\inbox\workbuddy\`：24 条，**11 条未读** → 全部按协议 `msg-cli.mjs read` 标记 read，复验「未读 0 条」。
- Agent Mail 连接器显示 connected，实际调用返回 `not_bound`（邮箱未开通）——需用户在「更多 - 我的邮箱」开通。
- **工具缺陷（已报豆包）**：豆包 09-17 09:03 那条 `info:脚本编码规范v1` 的 JSON 带 UTF-8 BOM，`msg-cli check` 解析失败并**静默跳过**，导致该条不显示在未读列表（我是逐文件读才发现的）。根因是 `check` 对解析失败静默 catch，BOM 只是触发条件 → 建议解析侧去 BOM + 解析失败要报错计数。已发 `correction` 类型消息给豆包，回读验证中文无乱码、outbox 留档正常。**注意：本机 JSON 消息切勿带 BOM**（PowerShell 5.1 `Out-File -Encoding UTF8` 会写 BOM，是嫌疑来源）。

## 二、环境踩坑（重要，每次会话都会遇到）

| 现象 | 解法 |
| --- | --- |
| bash 里 `ls`/`dirname`/`head` 找不到，PATH 缺失 | 每条命令前置 `export PATH="/usr/bin:/bin:/c/Windows/System32:$PATH"` |
| **PowerShell 工具无 stdout 返回**（只有 exit code） | 一律改用 bash |
| `node /c/Users/...` 被转成 `c:\c\Users\...` | 先 `cd` 到目标目录，再用相对路径调用 |
| `agent-browser` 未安装（装它要全局 npm + 500MB Chromium） | 直接用本机 Chrome：`"C:/Program Files/Google/Chrome/Application/chrome.exe" --headless=new --disable-gpu --hide-scrollbars --window-size=1440,2600 --virtual-time-budget=5000 --screenshot="<win路径>" "file:///..."` |
| `find . -newermt` 扫全工作台会超时 | 缩小范围或用 Grep 工具（ripgrep） |

## 三、验收：工作台模板与组件扩充（CodeBuddy 执行中）

产出 `08-文档\验收-模板与组件扩充-WorkBuddy-2026-09-17.md`，截图 `07-日志\验收-组件库gallery-20260917.png`。

- 任务 1（10 个品牌设计系统）✅ 通过；任务 2（业务页模板）⏳ 0 个；任务 3（组件库分类+机制）✅ 通过。
### ⚠️⚠️ 目录职责的权威口径（**用户 11:40 澄清，务必牢记，勿再混用**）

| 路径 | 职责 |
| --- | --- |
| `src\component-templates\axhub\` | **原型页面组件**（页面里 import 的高频 UI 组件）→ 新组件库唯一正确落点 |
| `src\components\axhub\` | **Make 客户端工具组件**（原型批注、发送修改上下文给智能体等）→ **不可放原型组件** |

> **我在此判反了**：曾把"工程里两个 axhub 目录"判为"落地路径没统一"的 **P1 硬伤**，并建议统一到 `components/axhub` → **判定与建议方向性错误，已全部撤回**。文档口径（README / 提示词手册 / gallery 三处写 `component-templates/axhub/`）**本来是对的**。
> **教训（今天第二次"现象对、归因错"）**：看到"两处都有 / 两处不一致"，**先问清各自职责归属，再下"没统一"的结论**。

### 修订后的问题清单（v2）

- 🔴 **A1 旧组件库残留，且占用了 Make 客户端组件的目录名**：`<工程>\src\components\axhub\`（旧 8 组件，09-13 23:00 建，涉及骨架/结构监测/崂山）+ `src\component-templates\Design-components\`（第二份副本）+ 6 个组件库浏览页（`basic/data/feedback/form/component-library/axhub-components`）仍 `import ... from '../Design-components'` → 没跟新库接上。建议：旧库退场、`components/axhub` 归位给 Make 客户端、浏览页 import 改指 `../axhub`。
- 🟡 **A2 新库迁移只覆盖 3/5**：骨架（49 文件，今日 11:01 落位）✅ / 指标管理平台 ✅ / 测试 ✅；结构监测 ❌ / 崂山 ❌ 仍是旧形态。
- P2 同名组件两套并存（Button/Card/StatCard/Tag/DataTable/Modal/ResultPage），旧库还导出 `ButtonProps/Column/StatTone` 等类型 → 迁移需保超集，替换前必须编译验证。
- P3 `server.mjs:1193` SKIP_NAMES 大小写缺陷（已实测复现）：`path.basename(src.toLowerCase())` 恒小写，集合里 `AUTHOR.txt`/`README.md` **永不命中** → 18+ 个模板的这两个文件会被拷进 `src/prototypes/<slug>/`。
- P4 品牌模板 `tw.css` 是空壳（只有 `@import "tailwindcss"` + TODO）且 index.tsx 未引用 → 死文件。
- P5 gallery 无真实渲染预览（只有名字+说明+复制提示词）。**建议**：内嵌 iframe 指向 vite 预览路由，或 headless Chrome 批量静态首帧截图。
- P6 分类口径需文档点明：`component-templates\` 下 6 个目录是**组件库浏览页**（非组件分类目录），真正的分类目录在 `component-templates\axhub\` 内的 `base/layout/form/data/feedback/business`；两者 `data/form/feedback` 重名易混。

### 已撤回的建议

- ~~建立"真源→项目"同步机制~~：用户明确真源模板面向**开发**、这部分计划剔除，**当前阶段专注原型**，暂不做同步机制。

## 四、可复用的验收方法（沉淀）

1. **注册表校验**：解析 `templates.json` → 必填字段 / id 唯一 / `sourcePath` 目录 / `coverPath` 文件 四条一次过。
2. **依赖登记真实性**：正则抽 `import` 的第三方包名，与 `extraDependencies` 对账（`react`/相对路径/`?url` 要排除）。
3. **类型检查（地面真相）**：不要用自建 tsconfig + paths 映射（会出假警报！）。**把组件库临时拷进任一有 `node_modules` 的工程内**，用该工程的 `typescript/lib/typescript.js` 建 Program，`lib: ['lib.es2020.d.ts','lib.dom.d.ts','lib.dom.iterable.d.ts']`、`jsx: ReactJSX`、`moduleResolution: Bundler`，另加 ambient d.ts 声明 `*.css`/`*.webp`/`*?url`。用完即删并回验。
4. **同步对账**：真源与项目副本做 md5 比对（本次 48 文件全同）。
5. **视觉核验**：Chrome headless 截图后**自己看图**（本次确认 gallery 渲染正常，并靠看图发现"卡片无预览"问题）。

## 五、被验证推翻的假设（教训）

- 曾推断 `data/index.ts` 显式重导出 `StatCard` + `business/index.ts` 也 `export *` 它 → 顶层双 `export *` 会造成 ESM 同名歧义、导致 import 失败。**写最小复现实测后被推翻**：两边解析到**同一个模块的同一绑定**，不算歧义，import 正常。
- 曾以为 `form/Input.tsx` 有 2 个真类型错误（`maxLength`/`value` 不存在于 `TextareaProps`）。**换用工程真实 node_modules 复测后归零**，是我第一版检查环境（paths 映射到 react 运行时包）造成的假警报。
- **教训：凡是"看起来是 bug"的推断，必须写最小复现或换环境交叉验证，否则容易误报。**

## 六、待办（用户已确认/待确认）

- [x] 清理 `C:\inbox`（唯一残留的空目录，`rmdir` 非递归删除 + 回验 GONE）
- [x] 缺陷（BOM 漏消息）已发豆包
- [ ] 回 ack：脚本编码规范 v1（UTF-8 带 BOM，仅限 `.cmd/.bat/.ps1`；JSON/MD 不写 BOM）
- [ ] 回 ack：触发路由规范 v0.2
- [ ] 平台重构接入清单 4 项（priority=high）
- [ ] 复核豆包的双工程模板修复 + 本地复测双 admin 端口（预期 3006→3007，绑 127.0.0.1）
- [x] 验收问题汇总（A1/A2 + P2–P6 + gallery 预览）已发豆包：`msg-cli send --type request_review`，to doubao、cc user、沿用原 thread_id，回读验证 3 副本无 BOM 中文完好（11:47）
- [x] **收尾工作全部交豆包排期**（用户 12:07 指令「都给豆包排期」）：`msg-cli send --type task --action schedule_planned_fixes`，含 A1/A2 + P2–P6 + gallery 预览 + 任务 2 建议做法；消息开头显式要求**以 A1/A2 为准、勿沿用我的 v1 P1**，并注明用户已授权排期。回读验证 3 副本无 BOM、action=schedule_planned_fixes（12:08）
- [x] 收到豆包 ack：**msg-cli BOM 缺陷已修复（v2.2）** —— 新增 `readJSON()` 统一去 `\uFEFF`；`check` 解析失败不再静默（打 `[PARSE_FAIL]`+计数）；09-协作 353 个 JSON 清出 16 个带 BOM；编码规范升 **v1.1**（`.cmd/.bat/.ps1` 带 BOM；`.json/.jsonl/.md/.ts/.tsx` **必须 UTF-8 无 BOM**；禁用 PS5.1 `Out-File -Encoding UTF8`）。**已实测修复生效**（`msg-cli read` 能正常解析带 BOM 消息）。CodeBuddy 亦于 02:15 独立报出同一缺陷（`markStatus` 崩溃），双路佐证。
