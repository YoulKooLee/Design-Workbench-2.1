# 产品设计工作台 v2 改版验收报告

> 验收方：CodeBuddy（独立核验，非改版执行方）
> 改版执行方：豆包
> 验收日期：2026-09-13
> 问题基线：`08-文档/产品设计工作台-v2-迭代方案.md` §2.1 问题清单 P1–P20 + §2.1 三处 v1.7 缺陷
> 方法：逐项读实物取证（非读方案自述）+ 实跑 `06-运行脚本/env-doctor.ps1` 与 `06-运行脚本/smoke-test.mjs`

---

## 0. 结论摘要

**已修 7 项 / 部分修 7 项 / 未修 7 项 / 新发现风险 8 条。**

一句话：**搬家与立账做得很干净（P5/P6/P7/P10/P11/P17 确实闭环），但"能跑的代码"与"讲规矩的文档"之间断了一层**——新增的 config、SMOKE、BOOTSTRAP 停在"文档层"，而真正决定行为的 `server.mjs` / `launch-project.ps1` 仍在按旧硬编码跑。所以**没有一项"启动链路类"核心隐患被真正消掉**。

最要害的三条：

1. **单一真源是假象（P9 未修）**：`workbench.config.json` 建了，但 `server.mjs` 与 `launch-project.ps1` **一行都不读它**。改配置不会改变任何运行时行为。
2. **安全边界写进配置却没生效（P16 未修）**：config 声明 `bindHost: 127.0.0.1`，而 `server.listen(PORT, cb)` **没传 host → 实绑 0.0.0.0**。面板无鉴权却能 spawn 进程、删目录。**这比不写更危险——会让人误判已经限定本机**。
3. **冒烟测试全绿但存在假绿（P13 部分修）**：实测 34/34 通过，可下面 22 条问题它一条都没抓到（含未修的 P16/P18/P19、含"三端假 ready"）。全绿给了虚假的收工信号。

---

## 1. 逐项验收矩阵

图例：✅ 已修　🟡 部分修　❌ 未修

### 1.1 启动链路类

| # | 基线问题 | 状态 | 实测证据 |
|---|---|:---:|---|
| P1 | 启动链路脆、失败静默 | 🟡 | ✅ `06-运行脚本/env-doctor.ps1` 新增；`07-日志/` 已有 `workbench-*.log` + `launch-*.log`。❌ 面板自身 stdout 仍写 `工作台面板/server-console.log`（`启动工作台.cmd:40`），未落 `07-日志`，违反 T11。❌ arm64 node 只给黄字警告不阻断 |
| P2 | NODE_OPTIONS 注入污染子进程 | 🟡→✅ | ✅ `server.mjs:46-59 cleanEnvForSpawn()` 完整（删 `NODE_OPTIONS` + 全部 `CODEBUDDY*` + PATH 过滤 CodeBuddy 目录）；实测 env-doctor 正确报红：`✗ NODE_OPTIONS 已被注入: --require="…/node-language-shim.cjs"`。❌ 但 `启动工作台.cmd` 起面板时没有 `set NODE_OPTIONS=`，清洗只覆盖 spawn 出去的子进程 |
| P12 | 补丁债务 | 🟡 | ✅ `04-维护台账/patches/` 三件齐（`make-cli.mjs.pre-patch.bak` **12,033,830 B ≈ 11.48 MB** 与基线数字一致、`apply-active-guard.mjs`、`patch-manifest.json`）。❌ manifest **无 `hash` 字段**（只有 size）；`backupDate` 是占位符 `"2026-08-xx"`。❌ env-doctor 只验"清单是否在位"，**不校验哈希、不检测补丁当前是否已应用**。方案 §3-B 要求的"哈希校验 + 一键重打 + 升级报警"三件套**未实现** |
| P13 | 零回归测试 | 🟡 | ✅ `smoke-test.mjs` 建立（5 段 34 项）。❌ 实测全绿但存在盲区（见 §3.1）。❌ `04-维护台账/tools/` **是空目录**，§3.3-E 的"收纳 + README 登记"未落地；`工作台面板/` 仍散落 `check-deps.mjs`（首行自述"临时…用完即删"，且第 3 行写死旧路径 `C:/Users/游翔/Documents/AI work/Axhub`）与运行期生成物 `launch-01-项目指标管理平台.tmp.ps1` |

### 1.2 配置与安全

| # | 基线问题 | 状态 | 实测证据 |
|---|---|:---:|---|
| P9 | 端口/路径静态写死 | ❌ | ⚠️ **最要害**。`workbench.config.json` ✅ 建立（ports/pools/bindHost/envClean 齐全），env-doctor ✅ 读它，smoke-test ✅ 读它。但：`server.mjs:22 const PORT = Number(process.env.AXHUB_MANAGER_PORT) \|\| 7788`、`:39 MAKE_ADMIN_PORT = … \|\| 53817`——**全文件检索 `workbench.config` 0 命中**；`launch-project.ps1:27 [int]$MakePort = 53817`、`:530 Find-FreePort 51720` 同样硬编码，**也不读 config**。改配置 → 运行时行为零变化 |
| P16 | 安全边界不明 | ❌ | config 写 `"bindHost": "127.0.0.1"`，但 `server.mjs:1637 server.listen(PORT, () => {…})` **无 host 参数 → Node 默认绑 0.0.0.0**；面板**全文件无鉴权**，却能 `spawn` 任意进程、`DELETE /api/projects` 删目录。config 里的 bindHost 是装饰品 |
| P15 | `.cmd` 编码约定与实测不符 | ❌ | 手册 §3 的编码约定**未回填**（见 P14）。`启动工作台.cmd` / `停止工作台.cmd` 标题仍是 `Axhub 工作台启动器/停止器`（品牌未改）。字节级 BOM/GBK **未实测**（见 §4 核验边界） |

### 1.3 记忆层

| # | 基线问题 | 状态 | 实测证据 |
|---|---|:---:|---|
| P3 | 记忆四分五裂 | 🟡 | ✅ `10-智能体记忆/` 已收敛：`000~004` + `rules/`(5) + `skills/`(4) + `agents/`(5 分区)。✅ `Agent_Stack` 内容已并入。❌ `10-智能体记忆/README.md:8-9` 仍引用**已不存在的** `Agent_Stack/06-reflection/反思协议.md` 与 `Agent_Stack/02-knowledge/` → 悬空引用。❌ `agents/` 下 5 个分区（豆包/CodeBuddy/WorkBuddy/DeepSeek/千问）**全为空壳目录**——与被诟病的 v1.7 `Agent_Stack/02-knowledge` 空壳**同型** |
| P10 | 记忆绑定单一厂商 | ✅ | 主干用中立的 `10-智能体记忆/`，各 agent 只占分区。根 `.workbuddy/` 仅存运行时状态 `workspace.json`（`停止工作台.cmd` 负责清），属运行时耦合而非记忆 |
| P11 | 新会话无自举入口 | ✅ | `10-智能体记忆/BOOTSTRAP.md` 建立，含「我是谁 / 工作台在哪 / 按场景读什么(9 条路由) / 触发路由摘要 / 铁律 T5–T19 / 快速体检命令」；与根 `README.md` 互相指向 |
| P19 | 大提示词文件 | ❌ | `02-模板/_project-template/AGENTS.md` **46.42 KB** + `AGENTS.template.md` **44.31 KB** **双份仍并存**；`01-项目/指标管理平台/` 同样两份。方案 §3.3-C 的"AGENTS.md 只留索引、正文挪 skills"**未做** |

### 1.4 目录卫生与文档

| # | 基线问题 | 状态 | 实测证据 |
|---|---|:---:|---|
| P5 | 目录卫生差 | ✅ | 根目录实测（`dir /a /b`）仅剩：`.gitignore`、`.workbuddy`、`01-项目`~`10-智能体记忆`、`package.json`、`README.md`、`workbench.config.json`、两个 `.cmd`、`工作台面板`。旧 `03-备份`/`04-临时`/`09-导出`/`.git`/`.org-manifest.txt` **全部清理**；8 个 `tmp.ps1`+9 个一次性 mjs 已不在面板目录（仅余 1 个 `check-deps.mjs` 与 1 个运行期 tmp）|
| P6 | 备份位置散 | ✅ | 根无 `03-备份/`；备份收敛到 `04-维护台账/{patches, archive/参考版底版}` |
| P17 | 二进制/合规杂物入库 | ✅ | 全库 `*.exe` **0 命中**（`handle.exe`/`handle64a.exe`/`Eula.txt` 已清）；根 `.git` **已无** |
| P20 | 目录编号名不副实 | 🟡 | ✅ 编号 `01~10` 已连续、`03-组件库`/`04-维护台账` 已归位。❌ 根 `README.md` 目录树把 `04-维护台账` 排在 `10-智能体记忆` **之后**（次序小瑕）；文档内仍写旧编号（见 P14）|
| P7 | 答疑手册空心化 | ✅ | `08-文档/工作台答疑手册.md` 现有「工具与脚本 / 工作流惯例 / 通用踩坑」3 栏目共 11 条实质问答，仅结尾留"（待继续积累）" |
| P14 | 文档—代码版本漂移 | ❌ | **三份文档全部未回填**：①`工作台架构与排错手册.md` 头部仍「最后核对 **2026-08-11**」，§1.1 仍写 `axhub-manager/server.mjs`，§1.3 仍写 `Axhub/` 根 + 旧编号；②`使用说明.md` 仍写 `axhub-manager/server.mjs`、`.org-manifest.txt`、`03-备份/`、`04-临时/`；③`TROUBLESHOOTING_LOG.md` 头部仍「记录于 `…\Axhub\`」|
| P4 | 前端单文件膨胀 | ❌ | `工作台面板/public/` **仍只有 `index.html` 一个文件**，实测 **1162 行**（旧基线 1042 行 → **反而长了 120 行**），末尾仍是 `render();`。注：根 `README.md` 已如实写明"单文件"，属**知情取舍**而非隐瞒 |

### 1.5 其余

| # | 基线问题 | 状态 | 实测证据 |
|---|---|:---:|---|
| P8 | 脚本编码纪律靠人肉记 | 🟡 | ✅ 规则已机读化：`10-智能体记忆/rules/rules.index.json` 有 trigger→rule 映射（R-SCRIPT/R-START/R-LAYOUT/R-FILE，含 scope 分级）+ `script-encoding.md`。❌ 与 `.cmd` 实测编码的对齐未做（依赖 P14 的手册回填）|
| P18 | 回收站有进无出 | ❌ | `05-回收站/` 为空（旧 `.deleted-*` 未迁，正确 ✅）。`server.mjs` 有 `moveToTrash()`（进得了）；但 `工作台面板/public/index.html` 全文检索 `回收站\|还原\|restore` → **0 命中**，**无任何还原入口**。方案 §3.3-D 的"回收站还原"未实现 |

---

## 2. v1.7 三处缺陷复核

| 缺陷 | 状态 | 证据 |
|---|:---:|---|
| ① `frame/index.json` 非法 JSON（`web`/`app` 丢键名、多一个 `}`）| ✅ **结构已修** | 实测 `frame/index.json` 为合法 JSON（version 1.0.1），`smoke-test` 与 `env-doctor` 均通过合法性校验 |
| ② README 承诺三端只实现一端 | ❌ **未修，且复杂化** | `03-组件库/frame/` 实测**只有 `admin/`**（无 `web/`、无 `app/`）。但三处仍在宣称存在，且**互相矛盾**：<br>· `frame/index.json` → web/app `"status": "ready"`<br>· `component-registry.json` → web/app `"status": "registered"`（有注："资源未随包交付时状态置 registered"）← **只有这里是对的**<br>· `frame/README.md` 正文（L37-38/76-77/84-85/186-187）与维护核对清单（L169 `包根存在 frame/{admin,web,app}`）均按"三端存在"写<br>**风险升级**：JSON 现在合法了，Agent 会真的读 `index.json` 按 `ready` 选型 → 直接踩空。而 smoke-test 只验"是合法 JSON"，放行 |
| ③ `frame/README.md` 引 `admin-usage.md` 悬空 | ❌ **未修** | `03-组件库/frame/` 下**无 `admin-usage.md`**，但 `frame/README.md:16` 与 `:181` 仍引用它 |

---

## 3. 新发现风险（8 条，不在原 P1–P20）

### 3.1 smoke-test 假绿（实测全绿掩盖真实缺口）

实测输出：`共 34 项检查：34 通过，0 失败 / 全部通过 ✓`

但它**只验"存在性/JSON 合法性"，不验"语义一致性"**，因此下面这些它一条都没抓到：

| 漏检项 | 说明 |
|---|---|
| `frame/index.json` 的 `ends` 与真实目录不符 | 只 `JSON.parse` 通过即算过 → 放行"web/app 假 ready" |
| `server.mjs` 不读 config（P9）无检查项 | — |
| `server.listen` 未绑 127.0.0.1（P16）无检查项 | 安全边界不在体检范围 |
| 回收站无还原入口（P18）无检查项 | — |
| `AGENTS.md` 46KB 未瘦身（P19）无检查项 | — |
| "旧路径残留"只扫 `09-协作/` | **不检查旧共享目录 `共享信息目录` 是否仍存在** → 见 §3.2 |
| 组件库版本号一致性 | `frame/index.json` v**1.0.1** vs `frame/admin/_meta/pages.catalog.json` v**1.1.0** vs registry v**1.1.0**，三处不一致，无检查 |
| `pageTypeCount` 与实际不符 | registry 写 `pageTypeCount: 40`，而实测 catalog 有 **50** 个 page-type（env-doctor 输出 `50 类页`、smoke-test 输出 `150 个引用`＝50×3）→ **registry 数字过期** |

另：smoke-test 的目录检查数组把 `04-维护台账` 排在末尾，输出里它落在"启动工作台.cmd 在位"之后，属小瑕。

### 3.2 共享目录被复制成两份（T12 违反）

| 路径 | agents | docs | messages | rooms | README |
|---|---|---|---|---|---|
| `C:\…\AI work\共享信息目录` | 15 文件 | 12 md | 310 文件 | 20 文件 | ✓ |
| `…\产品设计工作台\09-协作` | 15 文件 | 12 md | 310 文件 | 20 文件 | ✓ |

**逐项文件数完全相同 → 是整体复制而非移动**，违反 T12「共享目录协议不复制、不双写」/ T17「以 09-协作 为唯一真源」。真源分裂风险：

- 新副本 `09-协作/docs/01-协作规范/trigger-routing.md:48` 已把访问路径改为 `…\产品设计工作台\09-协作`
- 旧副本 `共享信息目录/…/trigger-routing.md` 仍指向旧路径
- 而各智能体的**跨会话长期记忆**（含我的）目前仍记「根 = `共享信息目录`」→ **两侧指令冲突**，谁改哪份都会与另一份漂移

> 按记忆约定：`共享信息目录` 工程由豆包统一维护，CodeBuddy 只有建议权。**此条需你决定由豆包排期，我不直接改。**

### 3.3 `03-组件库/frame/README.md` 整份相对路径失效

`frame/README.md` 是从 v1.7 包根布局整体拷来的，搬到 `03-组件库/frame/` 后**未重写相对路径**，几乎所有 `../` 链接悬空：

| README 引用 | 解析后实际位置 | 是否存在 |
|---|---|---|
| `./admin-usage.md` (L16, L181) | `frame/admin-usage.md` | ❌ |
| `../.agents/knowledge/ui-libs/arco-design-vue/` (L11, L158, L190-191) | `03-组件库/.agents/…` | ❌（实际在 `03-组件库/ui-libs/arco-design-vue/`）|
| `../.agents/skills/feature-dev/SKILL.md` (L9) | `03-组件库/.agents/…` | ❌ |
| `../admin/README-DEV.md` (L10, L188) | `03-组件库/admin/README-DEV.md` | ❌ |
| `./web/`、`./app/` (L186-187, L37-38) | `frame/web`、`frame/app` | ❌ |

### 3.4 Arco 规范仍无落点 —— 生成链路读不到

- `03-组件库/ui-libs/` 已补 **5 套**（`arco-design-vue` + ant-design-pro + element-plus + shadcn + vant，共 13 md，arco 有 `charts.md`/`components.md`/`pages.md`）✅ 方向对
- **但生成链路实际读的是模板侧的 `.agents/knowledge/ui-libs/`**（`frame/README.md:63` 写明加载顺序 `{PROJECT_PATH}/README-DEV.md` → `.agents/knowledge/ui-libs/`）
- 实测 `02-模板/_project-template/.agents/knowledge/ui-libs/` **只有 4 套：`ant-design-vue` / `element-plus` / `shadcn` / `vant` —— 无 `arco-design-vue`**
- 而交付端默认就是 arco（`frame/index.json` → `theme: "arco"`；`frame/admin/README-DEV.md`）
- → **arco 规范仍停在工作台侧副本，没接到 `page-generator` 的读取路径上**。这与我在《组件库迭代方案》§1.4 / 风险 V6 提的是同一个洞

### 3.5 `server.mjs` 分类目录清单过期

两处硬编码目录名单仍是旧结构：

```382:386:产品设计工作台/工作台面板/server.mjs
// 扫描项目时跳过的目录名（含整理后的分类文件夹，避免误扫/慢扫）
const EXCLUDE_DIRS = new Set([
  '_backups', 'tmp', '工作台面板', '.workbuddy', 'node_modules',
  '03-备份', '04-临时', '05-回收站', '06-运行脚本', '07-日志', '08-文档',
]);
```

```884:884:产品设计工作台/工作台面板/server.mjs
    const CATEGORY_NAMES = new Set(['01-项目','02-模板','03-备份','04-临时','05-回收站','06-运行脚本','07-日志','08-文档','_backups','tmp']);
```

- 列了**已不存在**的 `03-备份`、`04-临时`
- **缺** `03-组件库`、`04-维护台账`、`09-协作`、`10-智能体记忆`
- `CATEGORY_NAMES` 是 `DELETE /api/projects` 的**删除防护名单** → 上述分类目录**脱离删除保护**（构造请求即可被 `moveToTrash`）
- 缓解因素：`isProjectDir()`（`:445`，需 `.axhub/make` 或 `src/prototypes` 或 `package.json.name === '@axhub/make-client'`）挡了一层，这些目录不会自动出现在列表里。所以是**防护失效**，不是当下会误删

### 3.6 拆页版面板"手里有却没上"

`04-维护台账/archive/参考版底版/public/` 是 **`index.html` + `app.js` + `style.css` 三文件拆页结构**，且其 `server.mjs` **不含 7788 硬编码**（检索 0 命中）。而线上 `工作台面板/public/` 仍是单文件 1162 行。
→ **P4 的解法其实已经归档在手边**，只是没采用。建议评估：把这份拆页版接上当前 `server.mjs` 的接口，比继续往单文件里加代码划算。

### 3.7 根 `package.json` 是坏脚本

```1:13:产品设计工作台/package.json
{
  "name": "axhub-workbench",
  "version": "0.1.0",
  "private": true,
  "description": "Axhub 产品原型设计工作台：包装 Axhub Make，把项目创建、AI 接入、版本管理、技能、规则、目录导航外置到同一个页面",
  "type": "module",
  "scripts": {
    "start": "node server.mjs"
  },
```

- `server.mjs` 已挪到 `工作台面板/`，根目录下**没有** `server.mjs` → `npm start` 必失败
- `name` 仍是 `axhub-workbench`、`description` 仍写旧定位（品牌未去）
- 正确的一份在 `工作台面板/package.json`（`"start": "node server.mjs"`，name `工作台面板`）

### 3.8 arm64 / 非受控 node 隐患仍在（旧坑 #9 同型）

实跑 env-doctor：`✓ node v24.14.0 (arch=arm64)` → `⚠ 非 x64 架构，原生模块兼容性需注意`（**只警告，不阻断**）。
同时 `工作台面板/launch-01-项目指标管理平台.tmp.ps1` 把 `-NodePath` 指到 **豆包沙箱 node**：

```1:2:产品设计工作台/工作台面板/launch-01-项目指标管理平台.tmp.ps1
$OutputEncoding = [System.Text.Encoding]::UTF8; [Console]::OutputEncoding = [System.Text.Encoding]::UTF8;
& "C:\Users\游翔\Documents\AI work\产品设计工作台\06-运行脚本\launch-project.ps1" -RootDir "C:\Users\游翔\Documents\AI work\产品设计工作台" -ProjectName "01-项目\指标管理平台" -NodePath "C:\Users\游翔\AppData\Local\Doubao\User Data\sandbox_runtime\bases\c98c5042338ed152c6f10ecd8591889f\node\node.exe"
```

- 该 node 路径含**豆包沙箱 hash 目录**（`bases/c98c5042…`），**沙箱轮换即失效**
- 与 §2.1 旧坑 #9「Node 架构错配 → `Cannot find module @rollup/rollup-win32-*`」是同一类问题，且方案 §3.3-B 要求"受控 x64 node 前置到 PATH"**未落地**

---

## 4. 核验边界（未验项，如实声明）

1. `.cmd` 的**字节级编码**（UTF-8/BOM/GBK）未实测——只确认内容可正常读出、标题未改品牌。P15 的最终判定需按实际字节复核。
2. `launch-project.ps1` 是否**内联清洗** `NODE_OPTIONS` 未逐行核（仅确认它不读 config、端口硬编码）。
3. "面板删除项目 → 入回收站 → 还原"闭环未实跑（**还原入口缺失已由代码全文检索确认**，属功能不存在而非不工作）。
4. `01-项目/指标管理平台` 未实际起栈验证（本次只做静态核验，未触发 Vite/Make 启动流程）。
5. `共享信息目录` 与 `09-协作` 一致性仅按**文件数逐项比对**，未逐文件 diff 内容。
6. 未核 `10-智能体记忆/000~004` 正文与 v2 目录结构的引用是否全部有效（仅核了 `README.md` 与 `BOOTSTRAP.md`，已发现 `Agent_Stack` 悬空）。

---

## 5. 建议处理顺序

**A 类（拦路，建议立即修）**
1. `server.mjs` + `launch-project.ps1` 接入 `workbench.config.json`（否则 P9 永远只是文档）——顺便把 `server.listen(PORT, '127.0.0.1')` 一并改掉（P16）
2. `frame/index.json` 的 web/app 状态与 `component-registry.json` 对齐为 `registered`；`frame/README.md` 同步改为"仅 admin 已交付"
3. `smoke-test.mjs` 增加三条语义校验：`ends` 声明 ↔ 真实目录、registry `pageTypeCount` ↔ catalog 实际数、`bindHost` ↔ `server.listen` 第二参数

**B 类（文档回填，一次性）**
4. 手册 / 使用说明 / TROUBLESHOOTING_LOG 三份按新结构回填（P14/P15/P20）
5. `frame/README.md` 相对路径整体重写为 `03-组件库/` 布局（§3.3）
6. `10-智能体记忆/README.md` 去掉 `Agent_Stack` 悬空引用

**C 类（补齐功能）**
7. 回收站还原入口（P18）
8. 补丁哈希校验 + 一键重打（P12 后半）
9. `AGENTS.md` 瘦身（P19）
10. `04-维护台账/tools/` 收纳 `check-deps.mjs` 等散件并立 README

**D 类（需你裁定 / 走豆包排期）**
11. **共享目录双份**（§3.2）——需你决定唯一真源，由豆包执行，我不直接改
12. **arco 规范落点**（§3.4）——两条路：补 `02-模板/…/ui-libs/arco-design-vue/`，或改交付端默认库。**关联我在《组件库迭代方案》§9-D1 待你拍板的 D1-a/b/c**
13. 面板是否采用归档的拆页版（§3.6）
14. 根 `package.json` 坏脚本（§3.7）

---

*本报告由 CodeBuddy 独立核验产出，未修改工作台任何文件（仅读取 + 运行两个只读校验脚本）。*
