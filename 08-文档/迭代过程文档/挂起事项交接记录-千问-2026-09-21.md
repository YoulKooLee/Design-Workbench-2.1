# 工作台优化事项 · 挂起交接记录

> 记录：千问工作助理（qwen）｜日期：2026-09-21
> 背景：用户决定工作台后续由豆包接手，qwen 侧优化暂停。本记录基于 **09-21 只读盘点实测**（git、盘面状态、面板进程），非 09-19 旧快照。
> 状态标记：✅ 已闭环（09-20/21 复核确认）｜⏸️ 挂起（待用户或豆包处理）｜❓ 需复核（检测存在歧义）

---

## 一、✅ 豆包接手后已确认闭环的（无需再动）

| 项 | 证据（09-21 实测） |
|---|---|
| server.mjs EPIPE 自激防护 | `grep EPIPE` 命中；提交 `3a2e06d fix: 工作台卡住三根治`（09-20 08:52） |
| K2 组件真源定案 | 扁平中文分类结构落地（基础/布局/表单/数据展示/反馈/业务组件 + CMS/付款/工作流/触发器/订单/页面模式/_kit），`axhub/` 子层已不存在，gallery 断链随方案重构消失 |
| K1 `scr` 残留 | server.mjs 内已清（提交 `25f6e75`） |
| F1 Vibe 双副本 | 不删（组件库侧是预览运行时依赖，304 处相对引用）；Vibe Design Pro README 已含「双副本同步维护声明」 |
| 协作体系修复 | room-cli v0.3.2（B8 白板写入）稳定；qwen K6 admin 派生已入提交 `0a5721b` |
| 乱码目录 `07-鏃ュ織` | 09-21 实测已清理（工作台根已无此目录） |
| git 状态 | **未提交 0 条、未推送 0**——豆包已按约定完成 commit+push（09-19 时的"5 点后推送"遗留已了结） |
| 面板当前健康 | 7788 监听、2 秒 CPU 增量 0.00s、`/api/projects` 往返 99ms——卡住未发作 |

---

## 二、⏸️ 挂起事项清单（交豆包，按优先级）

### H1 · launcher.ps1 日志链路改造（原修复方案①未完成）— P1
诊断方案要求「管道重定向 → 文件重定向」，实测 `launcher.ps1` **仍是 `RedirectStandardOutput/Error` + `Start-Job ReadToEnd`**（L94-95、L102）。EPIPE 防护只保证面板进程不再自激，但**启动窗口关闭后日志仍会断流**（Job 终止、剩余输出丢弃）。影响降级为「日志不完整」，非卡住。修法：spawn 时 stdio 直接 `fs.openSync(logFile,'a')`。
（若豆包 3a2e06d 已换别的等效机制，此项可关——见第四节❓清单）

### H2 · 新建项目装配的响应性优化（原方案③，可能部分完成）— P1
09-19 卡住另一半根因：POST /api/projects 内 112 个主题目录**串行** robocopy（约 2.5 分钟同步冻结面板事件循环）。盘点显示装配代码结构已变（robocopy 改在 cpRetry 封装内），但**是否仍"一次请求串行做完 + 同步阻塞"未逐行确认**。验收点：新建期间 `/api/projects` 应保持 <100ms 响应、UI 有进度反馈。建议改法不变：合并为 1-2 次整树 robocopy + 装配挪后台、前端轮询状态。

### H3 · admin PACKAGING 的 vendor 装配契约说明（K3 尾款）— P2
豆包 09-19 承诺「补一段 README 说明谁/何时/拷哪些」，实测 `02-模板\_admin-template\templates\PACKAGING.md` **无 vendor 相关内容**。补一段即可关。

### H4 · admin 数据文件 BOM 清理 — P2
09-21 实测剩 **8 个**（09-19 为 14，豆包已清一批，.ps1/.sh 不在本次统计口径）。deepseek 已声明不认领、豆包口头"下轮处理"未排期。清单：
```
.agents\knowledge\conventions\agents.md
.agents\knowledge\conventions\workflow.md
.agents\knowledge\phase3-development\project.md
.agents\rules\session-context.md
.agents\rules\spawn-subagent.md
.agents\rules\srs-writer-workflow.md
.agents\rules\subagent-stop.md
.agents\skills\prototype-to-prd\references\axure-html-parse.md
```
（去 BOM 属格式修复、可逆，git 跟踪内随时回滚。）

### H5 · room-cli B9/B10 未修 + 存量脏数据 3 处 — P3
- B9：`room read` 按「参与者」精确匹配，中文显示名/括号注释/大小写混写导致漏列（09-19 实测 codebuddy 漏 50%）——归一化未做；
- B10：`handoff` 不校验接棒方是否合法成员（曾把 `--to`、`豆包` 写进白板）——校验未加；
- 存量脏数据 3 处 09-21 实测**仍未校正**：`CodeBuddy_工作台v2改版验收意见`、`CodeBuddy_左菜单规范未生效` 接棒人=「豆包」（应为 doubao），`CodeBuddy_结构监测PRD评审` 接棒人=codebuddy（最后 handoff 指向 doubao）。
建议顺序（deepseek 认同过）：先人工校存量 → 再加校验 → 回归。修 room-cli 前先与豆包对齐，避免并发改动（B8 教训）。

### H6 · （已闭环，编号保留占位）乱码目录 `07-鏃ュ織` 09-21 实测已清理，见第一节表格。

### H7 · 收件箱历史 unread 归档 — P3
qwen 收件箱积压约 16 封历史消息（09-05~09-19，验收测试/旧 task/ack），内容均已处理。跑一次 `archive-tool.mjs` 或逐条标 `replied/done` 后归档，避免新用户误读为待办。

### H8 · F1 收口措辞的"名分"问题 — 待用户一句话
deepseek 09-21 指出：豆包 K4 写的「用户拍板保持双副本」在消息系统里**没有 `from=user` 留痕**（用户原话只覆盖到"模板 vendor 保留"），按协议红线4属转述。实质结论不变（双副本保留是对的），只差定性：
- 路径 1：用户补一句留痕 → 定性"用户拍板"；
- 路径 2：qwen/豆包把终稿 §八 F1 措辞改为「依用户事实基准推导」→ 无需用户介入。
（qwen 倾向路径 2，改动仅文档措辞。）

---

## 三、⏸️ 更大范围挂起（用户与豆包之间的事，仅备案）

| 项 | 状态 |
|---|---|
| T2 三重审查整改 22 项 | 豆包 09-19 回报"全部落地"；deepseek 催的逐项复核（重点 server.mjs:200 await、spawn 兜底、.gitattributes、bootstrap.ps1、INDEX.md、硬编码路径）**未见复核回报落房** |
| T1 上下文工程终稿 N1-N6 | 豆包已落地（提交 `3f6efcc`）；deepseek 复验结论未落房 |
| deepseek 09-19 面板修复委托 | handoff 给 qwen，现由豆包实质完成（3a2e06d）——**房间未关闭**：`DeepSeek_面板坏管道自激循环修复` 仍 active、接棒人=qwen。建议豆包回报后 handoff→deepseek 复验关房，避免死循环挂账 |
| K5 smoke 55/55 确认 | 09-21 qwen 跑 smoke 时**未捕获到汇总输出**（可能全静默通过或超时），建议豆包贴一次完整 `smoke-test.mjs` 输出留档 |

---

## 四、❓ qwen 检测歧义项（豆包接手时顺手确认，勿当事实引用）

1. H1：若豆包用其他等效机制保住了窗口关闭后的日志（如 Start-Job 常驻/二级重定向），则"未修"判定作废；
2. H2：装配代码结构已变，串行同步 or 已异步需逐行看 `POST /api/projects` 现码；
3. K3：说明可能补在项目 README 而非 PACKAGING——qwen 未全盘检索。

---

## 五、qwen 侧承诺与边界

- 本记录落盘后，qwen **不再主动改动工作台任何文件**；上表 H1-H8 如需 qwen 代执行（尤其 H4 BOM 清理、H5 room-cli、H6 清理），由用户或豆包直接点名。
- 协作房内：qwen 名下无未回执阻塞项；`DeepSeek_面板坏管道自激循环修复` 房间的 handoff 处置见第三节，等豆包接。
- 本记录所在目录 `08-文档\` 为工作台文档层，后续豆包可视情归档或改名。

—— 千问工作助理 qwen，2026-09-21
